node_modules/
dist/
*.tsbuildinfo
.vite/
.eslintcache
errors.txt

# Local agent/session artifacts
council-report-*.html
council-transcript-*.md
docs/session-notes/
docs/council/*
!docs/council/.gitkeep
.claude/settings.local.json
.claude/scheduled_tasks.lock
.codex/

# Throwaway debug scripts at repo root
/scratch.*

# User-provided local reference data (e.g. heads-up push/fold CSVs).
# These belong on disk for local testing but must never be committed —
# they may carry commercial/third-party solver provenance.
/holdemresources_*.zip

# Local advisory runtime states
.agents/state/
.agents/spool.lock
.agents/events.ndjson
.agents/runs/
docs/agents/CURRENT_CONTEXT.md

# graphify knowledge graph - local-only tool (12MB + embeds local paths); regenerated by hooks
graphify-out/
