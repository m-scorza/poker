# Agent Handoff Archive — 2026-07

Older July 2026 handoff records moved out of `docs/agents/AGENT_HANDOFF.md` to keep the active baton inside the agent-kernel context budget.

## 2026-07-05 - Cron caveat QA18 focused export/privacy gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa18/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name/numstat inventory, active/archive handoffs, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and the current StudyPlan / StudyPlanCard / SpotSourcePanel / HandReplay / route-contract diffs. Chrome CDP at `127.0.0.1:9222/json/*` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton and rechecked the dirty slice's local SpotPacket/Data Health export boundary, caveat visibility, and copy/privacy posture; no code, product docs, source ledgers, private-site content, account/payment/upload/trainer-answer/solver-output, raw hand history, or tokenized URL handling was added.
- Verification:           Focused Vitest passed: `npx vitest run src/analysis/__tests__/studyPlan.test.ts src/analysis/__tests__/spotPacket.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose --pool=forks --maxWorkers=1` (`6` files / `55` tests). `npx tsc -b --pretty false` and `npm run privacy:check` passed. Added-line safety scan passed (`352` added shippable source/product-doc lines, `0` actionable brand/commercial/public-share/upload/solver-result/raw-private hits; helper quoting retries are logged). Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa18/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review, docs hygiene, or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA17 production build gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-build-qa17/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name/numstat inventory, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, and source/claims ledgers. Chrome CDP at `127.0.0.1:9222/json/list` was not reachable, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton and ran the production/PWA build gate on the current dirty slice; no code, product docs, source ledgers, private-site content, account/payment/upload/trainer-answer/solver-output, raw hand history, or tokenized URL handling was added.
- Verification:           `npm run build` passed (`tsc -b && vite build`; Vite transformed `3489` modules and generated the PWA service worker). Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Logs saved in `.agents/runs/2026-07-05-cron-build-qa17/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review, docs hygiene, or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA16 focused copy/IP recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-copy-risk-qa16/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name/numstat inventory, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, and source/claims ledgers. Chrome CDP at `127.0.0.1:9222/json/list` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton and ran a focused copy/IP/privacy scan over added `src/` and `docs/product/STATUS.md` lines; the refined scan found no blocking RegLife/PokerTrainer branded user-facing copy, commercial CTA/pricing/payment/public-sharing copy, state-changing external upload/trainer-answer/scoring claims, raw private payload fields, or positive solver-EV/frequency claims outside explicit negative/no-EV test assertions. Archived QA15 after the post-handoff kernel budget check showed the active baton had grown too large.
- Verification:           Focused dirty-slice tests passed: `npx vitest run src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose --pool=forks --maxWorkers=1` (`5` files / `38` tests). `npx tsc -b --pretty false`, `npm run docs:check`, and `git diff --check` passed. The first post-handoff `agentKernel` run failed only on `AGENT_HANDOFF.md` size (`7056 > 5120`); after archiving QA15, the final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Logs saved in `.agents/runs/2026-07-05-cron-copy-risk-qa16/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. The initial broad scan intentionally over-flagged source-ledger private-source labels and no-EV/no-score test assertions; the refined shippable-source/product scan passed. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review, docs hygiene, or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA15 full-suite gate recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-full-gate-qa15/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name/numstat inventory, active/archive handoffs, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and current dirty source/product/research/test footprint. Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton: the dirty slice still keeps payout/player-field/PKO Data Health caveat details visible, SpotSourcePanel overflow caveats reachable, and selected-hand/study-queue exports suggested-only/no-result/no-auto-upload/no-EV/no-trainer-scoring.
- Verification:           `npm test` passed (`78` files / `844` tests; existing Node localStorage warning only in the final excerpt). `npx tsc -b --pretty false`, `npm run build`, `npm run privacy:check`, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Initial broad added-line scan over source/research intentionally flagged source-ledger private-source labels and negative no-EV/no-score assertions; refined shippable source/product scan passed (`352` added lines, `0` actionable RegLife/GamePlan/payment/pricing/public-share/auto-upload/solver-result/trainer-answer/raw-private/token hits). Logs saved in `.agents/runs/2026-07-05-cron-full-gate-qa15/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies/results, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA14 focused/full gate recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa14/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name inventory, active/archive handoffs, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and the current StudyPlan / StudyPlanCard / SpotSourcePanel / HandReplay / route-contract source and docs diffs. Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton: the dirty slice still keeps payout/player-field/PKO Data Health caveat details visible, SpotSourcePanel overflow caveats reachable, and selected-hand/study-queue exports suggested-only/no-result/no-auto-upload/no-EV/no-trainer-scoring.
- Verification:           Focused Vitest passed (`5 files / 38 tests`, existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false`, `npm run build`, `npm run privacy:check`, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Refined added-line safety scan passed (`352` added shippable source/product-doc lines, `0` actionable RegLife/GamePlan/D#/dossier/payment/pricing/public-share/auto-upload/correct-answer/trainer-score/solver-result/raw-private/token hits). Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa14/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies/results, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA13 build/privacy recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa13/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name inventory, active/archive handoffs, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and the current StudyPlan / StudyPlanCard / SpotSourcePanel source/test diff. Chrome CDP at `127.0.0.1:9222` returned no JSON within the smoke-test window, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton: the existing slice still keeps payout/player-field/PKO Data Health caveat details visible, SpotSourcePanel overflow caveats reachable, and selected-hand/study-queue exports suggested-only/no-result/no-auto-upload/no-EV/no-trainer-scoring.
- Verification:           Focused Vitest passed (`5 files / 38 tests`, existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false`, `npm run build`, and `npm run privacy:check` passed. Broad source copy/privacy searches showed only pre-existing internal Game Plan comments/tests plus intentional no-EV/no-answer negative assertions; no patch was needed. Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed with `AGENT_HANDOFF.md` at 3965 bytes. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa13/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, reset, or change product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or a very small fix inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA12 focused gate recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research behavior change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa12/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name inventory, active handoff/archive, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and the active StudyPlan / StudyPlanCard / SpotSourcePanel diff. Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Confirmed the current dirty slice still focuses on concrete payout/player-field/PKO Data Health caveat visibility, selected-hand/study-queue source panels, suggested-only external-review targets, and no auto-upload/no-result/no-EV/no-trainer-scoring boundaries; archived QA11 to keep the active handoff under budget.
- Verification:           Focused Vitest passed (`5 files / 38 tests`, existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false`, `npm run build`, `npm run privacy:check`, and strict changed user-facing copy scan passed (`9` scoped files, `0` actionable RegLife/GamePlan/D#/dossier/payment/pricing/public-share/auto-upload/correct-answer/trainer-score hits). Initial post-entry `agentKernel` caught active handoff size over budget; after archiving QA11, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed with `AGENT_HANDOFF.md` at 4033 bytes. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa12/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not widen, rewrite, stage, commit, reset, or alter product behavior. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or a very small fix inside the already-dirty slice; do not widen to new product/research implementation until the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA11 full-suite/build/privacy recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa11/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, active handoff/archive, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, dirty source/test diffs, and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton: the caveat-detail slice still keeps concrete payout/player-field/PKO reasons visible in Data Health and SpotSourcePanel, keeps selected-hand exports suggested-only/no-result/no-upload, and the added-line safety scan found 0 actionable brand/account/payment/public-share/upload/solver-result/raw-private hits.
- Verification:           `npm test -- --reporter=dot` passed (78 files / 844 tests; existing jsdom `window.scrollTo()`, Node localStorage, and intentional rangeChecker warning notices only). `npx tsc -b --pretty false`, `npm run build`, `npm run privacy:check`, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` all passed. Added-line safety scan passed (356 added source/product/research lines, 0 actionable hits). Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa11/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, or reset that code. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA10 copy/privacy and typecheck recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa10/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, active handoff/archive, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, dirty source/test diffs, and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Stayed QA-only per the prior baton: the caveat-detail slice still keeps payout/player-field/PKO reasons visible in Data Health and SpotSourcePanel, keeps selected-hand exports suggested-only/no-result, and the added-line safety scan found 0 actionable brand/account/payment/public-share/upload/solver-result/raw-private hits.
- Verification:           Added-line safety scan passed (356 added source/product/research lines, 0 actionable hits). Focused Vitest passed (5 files / 38 tests; existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false` and `npm run privacy:check` passed. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed with active handoff under budget. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa10/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, or reset that code. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA9 full-suite and build recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, private-site extraction, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa9/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, active handoff/archive, task/handoff protocols, two-agent gate, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, representative dirty source/test files, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Because the prior baton asked not to widen the dirty slice, this tick stayed QA-only: the Data Health/Study Queue/SpotPacket caveat-detail work still presents concrete payout/player-field/PKO warnings, keeps selected-hand JSON export suggested-only/no-result, and changed source copy retains explicit no-EV/no-answer/no-upload/no-raw-hand boundaries.
- Verification:           `npm test` passed (78 files / 844 tests; existing jsdom `window.scrollTo()`, Node localStorage, and intentional rangeChecker warning notices only). `npm run build` passed and generated the production/PWA bundle. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed with active handoff under budget. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa9/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, rewrite, stage, commit, or reset that code. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA8 copy/privacy and typecheck recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to this handoff plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa8/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, active handoff, task/handoff protocols, two-agent gate, `CLAUDE.md`, current dirty source/product/research/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Reviewed the already-dirty caveat-detail slice as QA: concrete payout/player-field/PKO source-context reasons remain exposed through Data Health details and SpotSourcePanel overflow, selected-hand JSON exports stay suggested-only/no-result, and no user-facing changed source/product lines add RegLife/GamePlan/D#/dossier/commercial/public-share/secretish wording. A deliberately broad scan over research ledgers flagged expected internal RegLife/payment-account guardrail labels only; the refined user-facing/source scan passed.
- Verification:           Focused Vitest passed (5 files / 38 tests; existing jsdom `window.scrollTo()` notice only). `npm run privacy:check` and `npx tsc -b --pretty false` passed. Initial post-handoff `npm run docs:check` and `git diff --check` passed; `agentKernel` caught this file at 7196 bytes, so QA7 was archived. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed with active handoff under the 5120-byte budget. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa8/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-05 - Cron caveat QA7 build/privacy fallback recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-05-cron-caveat-qa7/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, task/handoff protocols, board, active handoff, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, current dirty source/product/research/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Reviewed the current caveat-detail slice as QA without widening it: Data Health source/context details preserve payout/player-field/PKO caveats, dashboard Data Health exposes queued caveat details, SpotSourcePanel keeps concrete ICM/PKO warnings visible before overflow, and selected-hand/route-contract JSON export tests still assert suggested-only target hints with no private result payloads.
- Verification:           Initial focused Vitest with `--pool=threads` failed before tests because all five workers timed out starting; fallback `--pool=forks --maxWorkers=1` passed (5 files / 38 tests; existing jsdom `window.scrollTo()` notice only). `npm run privacy:check` passed. `npm run build` passed. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=forks --maxWorkers=1` passed. Logs saved in `.agents/runs/2026-07-05-cron-caveat-qa7/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research remains blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron caveat QA6 focused regression recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-caveat-qa6/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, task/handoff protocols, board, active handoff, `CLAUDE.md`, `STATUS.md`, source/research ledgers, current dirty source/product/research/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` refused connection, so no authenticated/private tab inspection occurred. Reviewed the caveat-detail slice without widening it: Study Queue source-context summaries now keep all payout/player-field/PKO reasons, Dashboard exposes queued caveats in collapsed details, SpotSourcePanel keeps concrete ICM caveats visible before overflow, and Hand Replay/route-contract export regressions still assert suggested-only target hints with no private result payloads. Archived the prior QA5 baton after `agentKernel` caught the active handoff over the 5120-byte budget.
- Verification:           Focused Vitest passed: `npx vitest run src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose --pool=threads` (5 files / 38 tests; existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false` passed. Added-line shippable source/product-doc copy/IP/privacy scan passed with 0 actionable hits. Initial post-handoff `npm run docs:check` and `git diff --check` passed; `agentKernel` failed on active handoff size (7111 > 5120), QA5 was archived, then final docs/whitespace/agent-kernel checks passed. Logs saved in `.agents/runs/2026-07-04-cron-caveat-qa6/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron caveat QA5 full-suite recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive maintenance plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-caveat-qa5/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, task/handoff protocols, board, active handoff, `CLAUDE.md`, `STATUS.md`, current source/research/product/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` refused connection, so no authenticated/private tab inspection occurred. Reviewed the current dirty caveat-detail slice as QA and found no new code/doc patch needed: Data Health source/context details still expose the concrete payout/player-field/PKO caveats, StudyPlanCard keeps a collapsed full caveat list, SpotSourcePanel prioritizes ICM payout/player warnings before overflow, and selected-hand JSON export tests continue to assert suggested-only target hints without private result payloads.
- Verification:           `npm test` passed (78 files / 844 tests; existing jsdom `window.scrollTo()`, Node localStorage, and intentional rangeChecker warning notices only). `npx tsc -b --pretty false` passed. Added-line copy/IP/privacy scan over 356 added source/product/research lines passed with 0 actionable hits. Initial post-handoff `npm run docs:check` and `git diff --check` passed; `agentKernel` caught the active handoff at 6789 bytes, so the prior QA4 baton was archived, then final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=threads` passed. Logs saved in `.agents/runs/2026-07-04-cron-caveat-qa5/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron caveat QA4 focused recheck

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive docs plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-caveat-qa4/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, protocols/board, active/archive handoffs, `CLAUDE.md`, `STATUS.md`, current source/research/product/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Reviewed the current dirty caveat-detail slice as QA: Study Queue retains all payout/player-field/PKO source-context reasons, dashboard Data Health exposes queued caveats in collapsed details, `SpotSourcePanel` keeps concrete ICM/PKO warnings visible before overflow, and selected-hand JSON export tests assert suggested-only target hints without raw/private result payloads. Substantive patch IDs remain unchanged from the prior baton.
- Verification:           Focused Vitest passed with `--pool=threads` (5 files / 38 tests; existing jsdom `window.scrollTo()` notice only). `npx tsc -b --pretty false`, `npm run privacy:check`, and a refined added-line copy/IP scan over 356 added source/product/research lines passed with 0 actionable hits. Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=threads` passed (1 file / 5 tests). Logs saved in `.agents/runs/2026-07-04-cron-caveat-qa4/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron full Vitest QA3

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive docs plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-full-vitest-qa3/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, protocols/board, active handoff, `CLAUDE.md`, `STATUS.md`, current source/research/product/test diff, task spool state (`.agents/state/task_spool.json` absent), and Chrome CDP reachability. CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. The current dirty diff remains the Data Health / Study Queue / SpotPacket caveat-detail slice: all source-context reasons are retained, dashboard queued-caveat details are collapsible, `SpotSourcePanel` keeps concrete ICM/PKO warnings visible before overflow, and selected-hand JSON export tests assert target hints without raw/private result payloads. Current non-handoff patch IDs observed this tick: `src` `cdf4d69c09f63d35b797db57b633a84012c8391f`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`, tests `84d845196bc04bd5d8a509c963c62b010c1148b2`.
- Verification:           Full `npm test -- --pool=threads` passed (78 test files / 844 tests) in 229.88s. Existing noise only: jsdom `window.scrollTo()` notice, Node localStorage experimental warning, and the expected `rangeChecker` unknown-opener warning assertion. Post-handoff `npm run docs:check` and `git diff --check` passed; initial `agentKernel` caught the active handoff above 5120 bytes after adding this entry, so the prior baton was archived and final `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose --pool=threads` passed (1 file / 5 tests). Logs saved in `.agents/runs/2026-07-04-cron-full-vitest-qa3/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron Data Health caveat QA2

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change, no private-site extraction, and no cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive docs plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-datahealth-caveat-qa2/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/name-status, protocols/board, active/archive handoffs, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and task spool state (`.agents/state/task_spool.json` absent). Chrome CDP at `127.0.0.1:9222` was refused, so no authenticated/private tab inspection occurred. Reviewed the current dirty diff as QA: Data Health source-context reasons preserve full payout/player-field/PKO caveat details, Dashboard exposes queued caveats in a collapsed details list, `SpotSourcePanel` prioritizes concrete ICM/PKO warnings before overflow, and selected-hand JSON export tests assert target hints without raw/private result payloads. Current non-handoff patch IDs: `src` `2a4f8a0edf7b8ec9a3676b2cd078bb001525e420`, product `6a0e38ddfe5c2a64e732fe060f861a10f5d9ca27`, research `7c048fd83297034b4e3b5015f60eda04de8e3589`, tests `b038b306d5bd55d85bbdc2a08ffaaaf16aebc2a3`.
- Verification:           Focused Vitest passed with `--pool=threads` (5 files / 38 tests; existing jsdom `scrollTo` notice only). `npx tsc -b --pretty false`, `npm run build`, `npm run docs:check`, `npm run privacy:check`, `git diff --check`, and an added-line production safety scan passed. Logs saved in `.agents/runs/2026-07-04-cron-datahealth-caveat-qa2/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron Data Health export QA continuation

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Bounded QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No implementation, source-ledger, private-site, cron, Git history, account/payment/upload/trainer-answer/solver-result, screenshot, raw-private, or token action; tracked edits limited to handoff/archive docs plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-datahealth-export-qa/`. Existing dirty source/product/research/test/archive changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, current handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and task spool state (`.agents/state/task_spool.json` absent). Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Reviewed the existing dirty code diff as a QA slice: Study Queue Data Health keeps concrete payout/player-field/PKO caveats available, dashboard Data Health exposes first caveats plus full expandable details, Hand Replay `SpotSourcePanel` prioritizes concrete ICM/PKO caveats and keeps overflow caveats reachable, and selected-hand / route-contract SpotPacket export tests cover sanitized suggested-only target hints. Ran an added-line production safety scan over the changed production files; it scanned 107 added lines and found no high-risk production additions after allowing no-solver/no-upload caveat copy.
- Verification:           Focused Vitest passed with `--pool=threads` (5 files / 38 tests; existing jsdom `scrollTo` notice only). `git diff --check`, `npx tsc -b --pretty false`, `npm run docs:check`, `npm run privacy:check`, and the added-line production safety scan passed. Initial post-handoff `agentKernel` caught a 7279-byte active handoff; the prior baton was archived, then final `git diff --check`, `npm run docs:check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed with `AGENT_HANDOFF.md` at 4193 bytes. Logs saved in `.agents/runs/2026-07-04-cron-datahealth-export-qa/`.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim, widen, or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, public sharing path, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or very small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron Data Health caveat regression verification

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research implementation change this tick, and no private-site/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive docs plus gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-datahealth-caveat-regression/`. Existing dirty source/product/research/test changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat, protocols/board, active/archive handoffs, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and task spool (absent). Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Managed web readiness smoke still works (`web_search` plus public GTO Wizard Help extraction; source ledger already has `S-PUB-COMP-008`). Reviewed the current dirty diff as a bounded QA slice: source-context Study Queue reasons keep concrete payout/player-field/PKO caveats available, dashboard Data Health exposes collapsed/full caveat details, and Hand Replay `SpotSourcePanel` prioritizes concrete ICM/PKO caveats while moving overflow warnings into an expandable list. Current non-handoff patch IDs: `src` `491c91839e35f25fdef1d18e526d7c3f06b7f16c`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`, tests `84d845196bc04bd5d8a509c963c62b010c1148b2`.
- Verification:           Focused Vitest passed with `--pool=threads` (5 files / 38 tests; existing jsdom `scrollTo` notice only). `git diff --check`, `npx tsc -b --pretty false`, `npm run build`, `npm run docs:check`, and `npm run privacy:check` passed; logs saved in `.agents/runs/2026-07-04-cron-datahealth-caveat-regression/`. Added-line copy/IP scan found only expected private/tool source-ledger references and negated no-EV/no-answer/no-score assertions; no new account/payment/public-share/trainer-answer/solver-result path was found.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron Data Health caveat detail QA

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Data Health / Study Queue / SpotPacket caveat-detail diff. No source/product/research/private-site/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edit in this tick limited to this handoff, with gitignored verification logs under `.agents/runs/2026-07-04-cron-datahealth-caveat-qa/`.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-datahealth-caveat-qa/`. Existing dirty source/product/research changes were preserved.
- Summary:                Re-inspected `git status --short --branch`, diff stat/hash groups, protocols/board, active handoff, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and task spool (absent). Chrome CDP at `127.0.0.1:9222` timed out, so no authenticated/private tab inspection occurred. Managed web readiness smoke still works (`web_search` plus GTO Wizard Help extraction). Reviewed the current dirty diff as a QA slice: source-context Study Queue reasons now keep concrete payout/player-field/PKO caveats available, dashboard Data Health renders a collapsed local details list, and Hand Replay `SpotSourcePanel` prioritizes concrete ICM/PKO caveats while moving overflow warnings into an expandable list. Current non-handoff patch IDs: `src` `3c5cd31d7dc14a87c949d0ef8d7e3004dede1af9`, product `6a0e38ddfe5c2a64e732fe060f861a10f5d9ca27`, research `7c048fd83297034b4e3b5015f60eda04de8e3589`, tests `b038b306d5bd55d85bbdc2a08ffaaaf16aebc2a3`.
- Verification:           Focused Vitest passed with `--pool=threads` (5 files / 38 tests; existing jsdom `scrollTo` notice only). `npx tsc -b --pretty false`, `npm run build`, `npm run docs:check`, and `npm run privacy:check` passed; logs saved in `.agents/runs/2026-07-04-cron-datahealth-caveat-qa/`. Changed-diff copy/IP scan only found expected RegLife/GTO ledger rows and negated no-EV/no-answer/no-score test assertions. Initial post-handoff `agentKernel` exposed active handoff size over budget; archived the prior baton and final docs/whitespace/agent-kernel checks passed.
- Risks / assumptions:    Worktree remains intentionally dirty with substantive Data Health / Study Queue / SpotPacket work awaiting human review/commit; this tick did not claim or rewrite that code. CDP/private-site research is blocked until Chrome exposes a responsive debugging endpoint. No solver EV/frequencies, trainer answers/scoring, account data, payment flow, upload flow, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues before that happens, keep future ticks to bounded QA/copy-risk review or small fixes inside the already-dirty slice; do not widen to new product/research implementation unless the dirty work is committed/cleared or the user explicitly asks.

## 2026-07-04 - Cron current-diff verification and worker-timeout retry

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Study Queue / SpotPacket / Data Health / external-review diff. No source/product/research/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; tracked edits limited to handoff/archive docs and gitignored verification logs.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-04-cron-current-diff-qa/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/stat, protocols/board, active/archive handoffs, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, task spool (absent), CDP readiness (127.0.0.1:9222 refused), and managed web research smoke (GTO Wizard help search/extract OK). Current substantive patch IDs are `src` `b4abfdf98efe9a9307fbecefcb4f08e71c4f6ae9`, product `2e4b24aff0c3403ef9653d9a8cbd888d14dc4d1b`, research `778f6ec3ab515334694a99c3509d45c497273145`, tests `44cc798883b8a9c3180a9e292d52484d5e5d0146`; because these differ from the previous active baton, this tick treated the dirty diff as current-diff QA instead of widening implementation scope. Changed-file copy/IP scan found expected source-ledger RegLife/GTO references and negated no-EV/no-answer/no-score caveats/tests, with no new account/payment/public-share/trainer-answer/solver-result path.
- Verification:           Initial combined Vitest hit Windows/Vitest fork-worker startup timeouts after `SpotSourcePanel` passed (9 tests); sequential retry with `--pool=threads` passed the focused suite (5 files / 38 tests; existing jsdom `scrollTo` notice only). `npx tsc -b --pretty false`, `npm run build`, and `npm run privacy:check` passed. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Worktree remains intentionally dirty with Study Queue trust-substrate work awaiting human review/commit. No private-site extraction occurred because CDP was unavailable; no solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, local paths, or tokenized URLs were handled. The first Vitest failure appears environmental/worker startup rather than assertion failure and is covered by the successful sequential retry.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while these patch IDs are unchanged, keep future ticks to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-04 - Cron Study Queue caveat review QA refresh

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Study Queue / SpotPacket / Data Health / external-review diff. No source/product/research/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; no substantive code/doc patch beyond this handoff and gitignored evidence.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `/.agents/runs/2026-07-04-cron-studyqueue-caveat-review/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/stat, protocols/board, active handoff, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, task spool (absent), CDP readiness (127.0.0.1:9222 timed out), public research tool smoke (web search/extract OK), and the dirty StudyPlan/StudyPlanCard/SpotSourcePanel/route-contract diff. Substantive patch IDs still match the prior handoff: `src` `cdf4d69c09f63d35b797db57b633a84012c8391f`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`, tests `84d845196bc04bd5d8a509c963c62b010c1148b2`. Changed-file copy/IP search found no new Reg Life/GamePlan/payment/public-share language; solver/answer/score hits are expected no-EV/no-answer/no-score caveats/tests.
- Verification:           Focused Study Queue/SpotPacket/Data Health suite passed (5 files / 38 tests; existing jsdom `scrollTo` notice only). `npx tsc -b --pretty false`, `npm run privacy:check`, `npm run docs:check`, and `git diff --check` passed. Initial post-handoff `agentKernel` exposed active handoff size 6188 >5120, so the prior baton was archived; final post-handoff docs/whitespace/agent-kernel checks passed.
- Risks / assumptions:    Worktree remains intentionally dirty with the existing Study Queue trust-substrate work awaiting human review/commit. Since substantive patch IDs are unchanged, this tick stayed in verification/copy-risk review mode and did not widen implementation scope. No private-site extraction, solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while these substantive patch IDs are unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-04 - Cron unchanged Data Health caveat QA

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Study Queue / SpotPacket / Data Health / external-review diff. No source/product/research/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action; prior active baton archived to keep this file under budget.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored log `/.agents/runs/2026-07-04-cron-datahealth-caveat-qa/verification-summary.md`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/stat, protocols/board, active/archive handoffs, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, task spool (absent), CDP readiness (127.0.0.1:9222 timed out), and the dirty StudyPlan/StudyPlanCard/SpotSourcePanel/route-contract diff. Substantive patch IDs matched the prior handoff: `src` `cdf4d69c09f63d35b797db57b633a84012c8391f`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`, tests `84d845196bc04bd5d8a509c963c62b010c1148b2`. Reviewed copy/IP posture: strict source search hits were expected no-solver/no-answer negations or existing internal comments/metadata; no new commercial/public-share/private-token concern found.
- Verification:           `git diff --check` passed. Focused Study Queue/SpotPacket/Data Health suite passed (5 files / 38 tests; existing jsdom `scrollTo` notice only). `npx tsc -b --pretty false` passed. `npm run privacy:check` passed. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    Worktree remains intentionally dirty with existing Study Queue trust-substrate work awaiting human review/commit. Since substantive patch IDs are unchanged, this tick stayed in verification/copy-risk review mode and did not widen implementation scope. No private-site extraction, solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while these substantive patch IDs are unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-03 - Cron Data Health caveat diff QA

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA continuation on the existing dirty Study Queue / SpotPacket / Data Health / external-review diff. No source/product/research/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-03-cron-datahealth-caveat-review/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/stat, protocols/board, handoff, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, task spool (absent), and CDP readiness (127.0.0.1:9222 timed out). Substantive patch IDs are unchanged: `src` `cdf4d69c09f63d35b797db57b633a84012c8391f`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`, tests `84d845196bc04bd5d8a509c963c62b010c1148b2`. Reviewed the unchanged caveat diff: all source-context reasons stay available, dashboard details expand concrete payout/player-field caveats, SpotSourcePanel prioritizes ICM/PKO warnings with expandable overflow, and selected-hand export tests assert target hints plus sanitized no-result payloads. Added-line scan found 0 credential/token matches; RegLife/payment hits were private-source ledger notes; solver/EV/trainer hits were negated tests or `not_solver_backed` labels.
- Verification:           Focused Study Queue/SpotPacket/Data Health suite passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. `npm run privacy:check` passed. Initial post-handoff `agentKernel` failed only because active handoff was 6652 bytes >5120; after removing the prior active baton, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests). Logs under `.agents/runs/2026-07-03-cron-datahealth-caveat-review/`.
- Risks / assumptions:    Worktree remains intentionally dirty with existing Study Queue trust-substrate work awaiting human review/commit. Since substantive patch IDs are unchanged, this tick stayed in verification/copy-risk review mode. No private-site extraction, solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, local paths, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while these substantive patch IDs are unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-03 - Cron unchanged Study Queue diff re-verification

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review/QA continuation on the unchanged dirty Study Queue/SpotPacket/Data Health/external-review diff. This tick edited only handoff/archive docs and wrote gitignored logs; no source code, product/research ledger, cron, Git, account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/2026-07-03-cron-studyqueue-caveat-reverification/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status, protocols/board, current/archive handoffs, `CLAUDE.md`, `STATUS.md`, research inventory, task spool (absent), CDP readiness (127.0.0.1:9222 timed out), and the dirty source/docs diff. Substantive patch IDs remained unchanged from earlier QA ticks: `src` `cdf4d69c09f63d35b797db57b633a84012c8391f`, product `830984aa84857108f37c3e2a3cf40b3abcf91ba7`, research `a7f97e7816b0a5bb9196b5ed2432f90149166023`. The diff still centered on concrete ICM/PKO Data Health caveats, dashboard expandable caveat details, SpotSourcePanel caveat overflow, selected-hand SpotPacket export safety, and matching source/status docs. Changed-`src` scan found 340 added lines and 0 strict IP/commercial hits.
- Verification:           Focused Study Queue/SpotPacket/Data Health suite passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. `npm run privacy:check` passed. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Worktree remained intentionally dirty with the existing Study Queue trust-substrate work awaiting human review/commit. Because source edits were already uncommitted and unchanged, this tick deliberately avoided widening implementation scope. No private-site extraction occurred; no solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while these substantive patch IDs are unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-03 - Cron Study Queue caveat diff re-verification

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review/QA continuation on the dirty Study Queue/SpotPacket/Data Health/external-review diff. This tick edited only handoff/archive docs; no source code authored, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch status/diff, protocols/board, active handoff, `STATUS.md`, research ledgers, task spool (absent), and CDP readiness (127.0.0.1:9222 timed out). Reviewed the dirty source/docs diff: it still centers on Data Health source-context caveat expansion, dashboard caveat details, SpotSourcePanel warning overflow, selected-hand SpotPacket export safety tests, and shipped-state/source-claim ledger updates. Source patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`; product+research docs patch-id is `2a0303239b3517c48413f4176126053c05107cca`. Archived the prior baton after the first agent-kernel run exposed the active-handoff size budget. No private-site extraction occurred.
- Verification:           Focused Study Queue/SpotPacket/Data Health suite passed: 5 files / 38 tests. `npx tsc -b --pretty false` passed. `npm run docs:check` passed. `git diff --check` passed. Initial `agentKernel` failed only because `AGENT_HANDOFF.md` was 6199 bytes >5120; after archiving the previous baton, final `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests, active handoff 3681 bytes).
- Risks / assumptions:    Worktree remains intentionally dirty (13 files before this handoff update) with existing Study Queue trust-substrate work plus prior archive/handoff edits. Because source edits are already uncommitted, this tick deliberately avoided widening implementation scope. Search review found existing permitted boundary/caveat copy but no new pricing/payment/public-share/trainer-answer/solver-result path in the inspected diff. No solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while this patch-id is unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-03 - Cron focused diff verification and transient dirty-drift guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review/QA continuation on the dirty Study Queue/SpotPacket/Data Health/external-review diff. This tick edited only handoff/archive docs; no source code authored, cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch status/diff, protocols/board, active handoff, `CLAUDE.md`, `STATUS.md`, research ledgers, task spool (absent), and CDP readiness (127.0.0.1:9222 timed out this tick). Reviewed the current dirty Study Queue/Data Health caveat diff; final substantive patch IDs are unchanged from the prior guard (`src` cdf4d69, `docs/research` a7f97e7, `docs/product` 830984a). A transient mid-run status/read briefly showed extra private/local copy-neutralization files and one corresponding stale test expectation; by final status those files were no longer dirty and current Coach/Arena copy remains the baseline `The Arena`. No source patch was authored in this tick.
- Verification:           Focused Study Queue/SpotPacket/Data Health suite passed on rerun: 5 files / 38 tests. Current Coach/route contract suite passed after the transient copy drift resolved: 2 files / 12 tests. `npx tsc -b --pretty false` passed; `npm run privacy:check` passed; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Worktree remains intentionally dirty (13 files observed at final status) with the existing Study Queue trust-substrate work. Dirty set changed transiently during the run, so human review should confirm no parallel-process/provenance issue before commit. No private-site extraction occurred because CDP timed out; no solver EV/frequencies, trainer answers, account data, uploads, screenshots, raw hand histories, or tokenized URLs were handled.
- Next action requested:  Prefer human review/commit (or explicit cleanup) of the current dirty diff. If cron continues while this patch-id is unchanged, keep to verification/copy-risk review; do not widen implementation scope until the dirty work is committed/cleared or the user explicitly requests another slice.

## 2026-07-03 - Cron CDP refresh under unchanged dirty diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Read-only continuation while preserving unchanged dirty Study Queue/SpotPacket/Data Health/external-review diff; tracked edit limited to this handoff. No code/product/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored sanitized CDP probe script/artifact under `.agents/runs/20260703-073205-cdp-structural-noedit/`.
- Summary:                Dirty substantive patch IDs remained unchanged (`src` cdf4d69, `docs/research` a7f97e7, `docs/product` 830984a); task spool file was absent. CDP was reachable with 8 targets; sanitized read-only probe inspected only GTO Wizard `/solutions` structure (2,420 elements, 535 data-attribute nodes, 8 visible buttons, 21 visible links). RegLife lesson and PokerTrainer tabs again timed out; login/chat/search/video targets were skipped.
- Verification:           Sanitized artifact title/URL query-token check passed; post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Existing dirty app/product/research diff is preserved and still needs human review/commit; no product behavior changed. Private-tool evidence remains workflow/testability abstraction only, with no solver EV/frequencies, trainer answers, screenshots, storage/cookies, account data, uploads, or raw tokenized URLs extracted.
- Next action requested:  Continue read-only verification or human-review support until the dirty diff is committed/cleared, unless the user explicitly expands implementation scope; if unchanged, the best next tick is a focused diff review of the existing Study Queue external-review changes rather than another duplicate CDP probe.

## 2026-07-03 - Cron read-only CDP and dirty-diff guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Read-only/status continuation on unchanged dirty Study Queue/SpotPacket/Data Health/external-review diff; tracked edit limited to this handoff. No code/product/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored sanitized CDP artifact under `.agents/runs/20260703-075000-cron-cdp-readonly/`.
- Summary:                Confirmed substantive patch IDs unchanged (`src` cdf4d69, `docs/research` a7f97e7, `docs/product` 830984a). CDP remained reachable; GTO Wizard `/solutions` returned structural counts only (2,420 elements, 535 data-attribute elements, 9 buttons, 24 links). RegLife lesson/PokerTrainer timed out; login/chat/search/video tabs skipped.
- Verification:           Initial post-entry `agentKernel` exposed handoff size 5184 > 5120, so the prior baton was archived; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Existing dirty app/product/research diff is preserved and still needs human review/commit; no product behavior changed. Private-tool evidence remains workflow/testability abstraction only.
- Next action requested:  If patch IDs remain unchanged, keep future cron ticks to read-only verification or human-review support until the dirty diff is committed/cleared or the user explicitly expands implementation scope.

## 2026-07-03 - Read-only focused verification on unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review/QA-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored verification/CDP artifacts under `.agents/runs/20260703-063212-cron-readonly-verify/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, dirty diff stats, current patch IDs, and Chrome/CDP reachability. The substantive `src` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`; `docs/research` remains `a7f97e7816b0a5bb9196b5ed2432f90149166023`; `docs/product` remains `830984aa84857108f37c3e2a3cf40b3abcf91ba7`. Ran a read-only structural CDP retry after a first probe expression returned no object value: GTO Wizard `/solutions` was inspectable (2,420 elements, 535 data-attribute elements, 9 buttons, 24 links); RegLife lesson and PokerTrainer timed out; GTO login was skipped as sensitive.
- Verification:           Focused dirty-diff coverage passed: `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx --reporter=verbose` = 5 files / 38 tests passed. `npx tsc -b --pretty false` passed. Initial post-handoff `agentKernel` exposed the handoff-size budget (6127 > 5120), so the prior baton was archived; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    No product behavior changed. The branch remains intentionally dirty with pre-existing Study Queue/SpotPacket/Data Health/external-review work awaiting review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral; external private tabs remain opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues while the substantive patch IDs are unchanged, avoid widening code scope; keep to read-only drift/verification or report no new product change unless the user clears/commits the current dirty work or explicitly expands scope.

## 2026-07-03 - Read-only review digest on unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review/QA-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored digest script/artifacts under `.agents/runs/20260703-060306-cron-review-digest/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, archived/current handoffs, dirty diff stats, current patch IDs, and Chrome/CDP reachability. Generated a gitignored human-review digest (`qa_digest.md`/`.json`): `src` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`, `docs/research` patch-id remains `a7f97e7816b0a5bb9196b5ed2432f90149166023`, 340 changed-`src` added lines scanned with 0 actionable RegLife/GamePlan/D#/dossier/payment/pricing/public-share hits, and the digest artifact scan found 0 private URL/token markers. CDP was reachable with 8 tabs / 7 sanitized tabs; GTO Wizard `/solutions` structural metadata only was inspectable (2,420 elements, 535 data-attribute elements, 9 buttons, 24 links); RegLife lesson and PokerTrainer timed out; GTO login was skipped as sensitive.
- Verification:           `python .agents/runs/20260703-060306-cron-review-digest/readonly_review_digest.py` passed and wrote the sanitized digest. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    No product behavior changed. The branch remains intentionally dirty with pre-existing Study Queue/SpotPacket/Data Health/external-review work awaiting review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral; external private tabs remain opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues while this patch-id is unchanged, avoid widening code scope; keep to read-only drift/verification or report no new product change unless the user clears/commits the current dirty work or explicitly expands scope.

## 2026-07-03 - Focused verification on unchanged Study Queue external-review diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Read-only verification/CDP continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifact under `.agents/runs/20260703-050400-cron-readonly-verify/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, uppercase research ledgers, source/claim IDs, and CDP reachability; web search/extract smoke passed against public GTO Wizard help. The `src/` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. Read-only CDP found 8 tabs and probed only sanitized structural metadata: GTO Wizard `/solutions` remained inspectable (2,420 elements, 1,690 data-attribute elements, 535 `data-tst` attrs, 9 buttons, 24 links); RegLife lesson and PokerTrainer page targets timed out; GTO login was skipped as sensitive. The artifact omits raw URLs, query strings, titles, text content, selector values, account data, and WebSocket URLs.
- Verification:           Focused Vitest passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. Artifact scan found no raw URL/WebSocket/query markers. Refined changed-`src` gate scan found 0 actionable RegLife/GamePlan/D#/payment/pricing/public-share hits (one route-fragment false positive). Initial post-handoff `agentKernel` exposed the handoff-size budget (5634 > 5120), so the prior baton was archived; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs/artifact are under `.agents/runs/20260703-050400-cron-readonly-verify/`.
- Risks / assumptions:    No product behavior changed. The branch remains intentionally dirty with pre-existing Study Queue/SpotPacket/Data Health/external-review work awaiting review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral; external private tabs remain opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues while this patch-id is unchanged, avoid widening code scope; keep to read-only drift/verification or report no new product change unless the user clears/commits the current dirty work or explicitly expands scope.

## 2026-07-03 - Read-only drift guard on unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification/read-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored structural probe script/artifact under `.agents/runs/20260703-043153-cron-readonly-drift-verify/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and CDP readiness. The `src/` patch-id is still `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. Read-only CDP listed 8 tabs; structural probing succeeded only for GTO Wizard `/solutions` (2,420 elements, 535 data attributes, 9 buttons, 24 links), timed out on RegLife/PokerTrainer, and skipped login/account targets. The artifact redacts lesson/trainer paths and a scan found no actual `http(s)` URLs, query strings, or known lesson/trainer IDs. Changed-`src` gate scan found no RegLife/GamePlan/payment/pricing/public-share wording.
- Verification:           `npm run docs:check` passed; `git diff --check` passed; `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests). Logs are under `.agents/runs/20260703-043153-cron-readonly-drift-verify/`.
- Risks / assumptions:    No product behavior changed; the branch remains intentionally dirty awaiting human review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral; external private tabs remain opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues and this patch-id is unchanged, avoid widening code scope; report no new product change or keep to read-only drift/verification only.

## 2026-07-03 - Read-only CDP drift check on unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification/read-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored structural probe under `.agents/runs/20260703-cron-readonly-cdp-refresh/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/protocols/product status/source ledgers. The `src/` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. Chrome CDP listed 8 tabs / 4 candidate private-tool pages; read-only structural probing succeeded only for GTO Wizard `/solutions` (2,420 DOM elements, short selector/control inventory) and timed out on the RegLife lesson, RegLife PokerTrainer, and GTO login targets. The probe stored host/path only and did not read cookies/storage, take screenshots, click, upload, access account/payment settings, submit trainer answers, or extract solver frequencies/ranges.
- Verification:           `npm run docs:check` passed; `git diff --check` passed; `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    No product behavior changed; the branch remains intentionally dirty awaiting human review/commit. The CDP artifact is Gitignored and is private/tool workflow evidence only, not a new public/product claim. RegLife/PokerTrainer CDP access remains opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues and this patch-id is unchanged, keep to read-only drift checks or verification; do not widen code scope until the current dirty diff is reviewed.

## 2026-07-03 - Broad verification of unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/probe under `.agents/runs/20260703-040000-cron-broad-verify/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected status/protocols/docs/ledgers. The `src/` patch-id is still `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. Read-only CDP captured sanitized structure for GTO Wizard `/solutions` only (2,420 elements, 535 data-attribute elements); RegLife/PokerTrainer/login targets timed out without storage, screenshots, upload, account/payment, trainer-answer, or solver-output access.
- Verification:           `npm test` passed (78 files / 844 tests); `npm run build` passed (`tsc -b` + Vite/PWA). Initial post-handoff `agentKernel` exposed handoff size 5927 > 5120; after archiving the prior baton, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    No product behavior changed; the branch remains intentionally dirty awaiting human review/commit. Build output remains untracked/ignored. GTO Wizard is private/tool workflow evidence only; RegLife is licensed-private/brand-neutral.
- Next action requested:  Prefer human review/commit. If cron continues and this patch-id is unchanged, keep to read-only drift checks or verification; do not widen code scope until the current dirty diff is reviewed.

## 2026-07-03 - No-change dirty diff verification + CDP probe

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff. No source-code edits, cron changes, commits, resets, deletes, pushes, account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored artifact `.agents/runs/20260703-030327-cron-cdp-verify/cdp_structural_probe.json`. Existing dirty app/product/research/archive files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and dirty diff stats. The `src/` patch-id still equals `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. Read-only CDP reached GTO Wizard `/solutions` (2,420 elements, 535 stable data attributes) and recorded only structural selector/control counts; RegLife lesson and PokerTrainer tabs were present but timed out under read-only WebSocket attach. A diff text scan flagged only expected research-ledger references and negative no-EV/no-solver assertions, not a new pricing/public-share/product-code scope.
- Verification:           `npx vitest run src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    No product behavior changed in this tick; the branch remains intentionally dirty with accumulated Study Queue/SpotPacket/Data Health/external-review work awaiting human review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral. The GTO probe saw upgrade/payment labels but did not interact with account, subscription, upload, trainer, or solver-result surfaces.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues and this patch-id is unchanged, keep to read-only drift/verification or a broad verification pass; do not widen code scope until the current dirty diff is reviewed.

## 2026-07-03 - Privacy/CDP gate on unchanged Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA/read-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff; no code/product-doc/research-ledger/cron/Git/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifact under `.agents/runs/20260703-023316-cron-privacy-cdp-gate/`. Existing dirty app/product/research files were preserved.
- Summary:                Re-inspected branch/status, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, dirty diff stats, current `src/` patch-id, and Chrome/CDP reachability. The `src/` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`, so code scope was not widened. CDP structural probing again succeeded only for GTO Wizard `/solutions` (2,420 elements, 1,690 data-attribute elements, 535 `data-tst` attrs, 9 buttons, 24 links); RegLife lesson and PokerTrainer timed out; GTO login was skipped as sensitive. Added a private/local gate pass: privacy boundary check passed, the privacy-boundary regression suite passed, changed-`src` added-line scan found 0 actionable RegLife/GamePlan/D#/dossier/payment/pricing/public-share hits, and the CDP artifact redaction scan found 0 raw URL/token markers.
- Verification:           `npm run privacy:check` passed; `npx vitest run src/__tests__/privacyBoundaryCheck.test.ts --reporter=verbose` passed (1 file / 4 tests); changed-`src` gate scan passed; CDP artifact redaction scan passed. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs/artifact are under `.agents/runs/20260703-023316-cron-privacy-cdp-gate/`.
- Risks / assumptions:    No product behavior changed. The branch remains intentionally dirty with pre-existing Study Queue/SpotPacket/Data Health/external-review work awaiting review/commit. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private/brand-neutral; external private tabs remain opportunistic and should not gate local QA.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues while this patch-id is unchanged, avoid widening code scope; keep to read-only drift/verification or report no new product change unless the user clears/commits the current dirty work or explicitly expands scope.

## 2026-07-03 - No-change CDP/source drift guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Read-only continuation on the existing dirty Study Queue/SpotPacket/Data Health/external-review diff. No source-code edits, cron changes, commits, resets, deletes, pushes, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored artifact `.agents/runs/20260702-233251-cron-source-drift-guard/cdp_structural_probe.json`. Existing dirty app/product/research/archive files were preserved.
- Summary:                Re-inspected `git status --short --branch`, protocols/board, active handoff, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and current source patch-id. Current `src/` patch-id remains `cdf4d69c09f63d35b797db57b633a84012c8391f`, matching the prior handoff, so no product code scope was widened. Read-only Chrome/CDP probe found GTO Wizard `/solutions` still reachable with 535 `data-tst` attributes, 43 selector-family prefixes, 9 buttons, 24 links, 2 role nodes, and Study/Practice/Analyze/Upload nav text; RegLife lesson/PokerTrainer tabs were present but not touched beyond sanitized tab listing. Login/account/payment/upload/trainer-answer routes and solver outputs were skipped.
- Verification:           Initial `agentKernel` exposed the active-handoff size budget (5359 > 5120), so the prior baton was archived. Final logged `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (5 tests); logs under `.agents/runs/20260702-233251-cron-source-drift-guard/`.
- Risks / assumptions:    No product source behavior changed in this tick; branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff awaiting human review/commit. GTO Wizard evidence remains private/tool workflow abstraction only; RegLife remains licensed-private/brand-neutral.
- Next action requested:  Prefer human review/commit of the existing diff. If cron continues and the `src/` patch-id remains unchanged, avoid widening code scope; use only read-only drift checks or report no new product change unless a real diff/test/source change appears.

## 2026-07-03 - No-change drift guard for Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA/readiness-only continuation on the already-dirty Study Queue/SpotPacket/Data Health/external-review diff. No source-code edits, cron changes, commits, resets, deletes, pushes, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored artifacts under `.agents/runs/20260703-020325-cron-nochange-drift/`. Existing dirty app/product/research files were preserved.
- Summary:                Inspected current status/diff stat, active and archived handoffs, protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and Chrome/CDP. Current source/test diff patch-id is `cdf4d69c09f63d35b797db57b633a84012c8391f`; docs/process diff patch-id before this handoff update was `907955d3d0eba0abe2b9645585a4acbabf46d27a`. Read-only CDP structural probe confirmed GTO Wizard `/solutions` still exposes 535 `data-tst` attributes, 9 buttons, and 2 role nodes; RegLife lesson and PokerTrainer timed out under read-only Runtime attach; login/account/payment/upload/trainer-answer routes were skipped. Changed-`src` copy scan found only route-fragment `#` false positives for `D#`, with no RegLife/GamePlan/dossier/payment/pricing/public-share hits.
- Verification:           Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    No product source behavior changed in this tick; branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff awaiting human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit using the existing checklist. If cron continues and the product diff patch-id remains unchanged, avoid widening code scope; report no new product change or rerun gates only after a real diff change.

## 2026-07-02 - Verification refresh for Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the already-dirty Study Queue/SpotPacket/Data Health/external-review diff. No source-code edits, cron changes, commits, resets, deletes, pushes, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored artifacts under `.agents/runs/20260702-cron-verification-refresh/`. Existing dirty app/product/research files were preserved.
- Summary:                Inspected status/diff inventory, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, changed Study Queue/SpotSourcePanel diff, and sanitized Chrome/CDP tabs. Ran a read-only structural CDP probe: GTO Wizard `/solutions` remained inspectable with structural selector/control counts only; RegLife lesson/PokerTrainer/GTO login targets timed out under read-only attach. No private DOM text, screenshots, storage/cookies, solver frequencies, trainer answers, uploads, account/payment/login flows, raw hand histories, villain names, or tokenized URLs were retained.
- Verification:           Focused Vitest passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. `npm run build` passed. Post-handoff `npm run docs:check` and `git diff --check` passed; initial `agentKernel` exposed the handoff-size budget (5876 > 5120), so the prior baton was archived. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (5 tests).
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff awaiting human review/commit. This tick verified but did not modify source behavior. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the existing diff. If cron repeats before review and the diff is unchanged, avoid widening scope; rerun gates only if files changed, otherwise report no new product change.

## 2026-07-02 - Human diff-review checklist for Study Queue diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the already-dirty Study Queue/SpotPacket/Data Health/external-review diff. No source-code edits, cron changes, commits, resets, deletes, pushes, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored checklist/logs under `.agents/runs/20260702-diff-review-checklist/`. Existing dirty app/product/research files were preserved.
- Summary:                Inspected status/diff inventory, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, source/test/doc diffs, and sanitized Chrome/CDP tab inventory. Created `.agents/runs/20260702-diff-review-checklist/review-checklist.md`, a human review checklist covering StudyPlan source-context reasons, Dashboard Data Health caveat expansion, SpotSourcePanel overflow, Hand Replay/SpotPacket target hints, export privacy, and commit-review sequence. CDP was used only for sanitized tab inventory; no private DOM/result payloads, uploads, trainer-answer actions, or account/payment/login flows were touched.
- Verification:           Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. No source tests were rerun because this tick added only a gitignored checklist plus handoff/archive updates; the prior active baton records focused Vitest, typecheck, build, docs, diff, and gate-copy scans for the unchanged product diff.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff awaiting human review/commit. This run is review-aid only and does not add solver EV/frequencies, payout/bounty values, trainer answers/scoring, uploads, account/payment flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history changes. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit using the checklist. If cron continues before review and the diff is unchanged, avoid widening scope; rerun gates only if files changed, otherwise report no new product change.

## 2026-07-02 - Accumulated Study Queue diff QA sweep

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only continuation on the already-dirty Study Queue/SpotPacket/Data Health/external-review diff. No cron changes, source-code edits, commits, resets, deletes, pushes, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs under `.agents/runs/20260702-qa-continuation/`. Existing dirty app/product/research files from prior ticks were preserved.
- Summary:                Inspected `git status`, diff stat/name inventory, current handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and sanitized Chrome/CDP tab inventory. CDP was reachable with authenticated GTO Wizard Solutions, RegLife lesson, and PokerTrainer tabs visible; this tick only recorded sanitized host/path/title inventory and did not inspect strategy outputs, submit trainer answers, upload files, or enter account/payment flows. Re-ran the accumulated diff's verification gates without changing source behavior, then archived the prior baton after `agentKernel` exposed the active-handoff size budget.
- Verification:           Focused Vitest passed (`npx vitest run src/__tests__/studyQueueRouteContract.test.tsx src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx --reporter=verbose`: 5 files / 38 tests, existing jsdom `scrollTo` notice); `npx tsc -b --pretty false` passed; `npm run build` passed; changed-`src` gate-copy scan found 0 RegLife/GamePlan/D#/dossier/payment/public-share hits; `npm run docs:check` passed; `git diff --check` passed. Initial post-handoff `agentKernel` failed on size budget (6294 > 5120); after archive compaction, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs are in `.agents/runs/20260702-qa-continuation/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff. This run adds verification evidence only; it does not add solver EV/frequencies, payout/bounty values, trainer answers/scoring, uploads, account/payment flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history changes. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues first before review, keep the next slice QA-oriented and non-invasive: produce/fill a concise human diff-review checklist or rerun gates only if files changed.

## 2026-07-02 - Data Health caveat full-list guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Tiny continuation on the already-dirty Study Queue/Data Health caveat diff. Read-only sanitized CDP only; no cron changes, Git commit/reset/delete/push, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/analysis/studyPlan.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifact under `.agents/runs/20260702-2104-cron-source-caveat-qa/`. Existing dirty app/product/research files from prior ticks were preserved.
- Summary:                Inspected `git status`, diff stat/name inventory, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and the active StudyPlan/StudyPlanCard/SpotSourcePanel/test diffs. CDP was reachable; GTO Wizard `/solutions` returned sanitized structural counts only (`535` `data-tst`, `9` buttons, `2` roles), while RegLife and PokerTrainer read-only Runtime probes timed out. Fixed the Data Health source-context evidence summary so it no longer truncates the actual caveat list at the data layer and prioritizes concrete payout/player/field/PKO review inputs before legacy/parser-source reasons; the dashboard still owns visual `+N more` overflow. Updated the regression from “hidden count” to “all reasons available,” including ordering before legacy source caveats.
- Verification:           Focused Vitest passed (5 files / 38 tests, existing jsdom `scrollTo` notice; log `focused_vitest.log`); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:check` passed; `git diff --check` passed; final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs are in `.agents/runs/20260702-2104-cron-source-caveat-qa/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health/external-review diff. This run is a small correctness guard inside that diff; it does not add solver EV/frequencies, payout/bounty values, trainer answers/scoring, uploads, account/payment flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history changes. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues first, avoid broad feature surface; next safe slice is review/QA only (for example a final human-oriented diff summary or rerun gates only if the diff changes).

## 2026-07-02 - QA refresh on unchanged Study Queue external-review diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health/external-review diff. Tracked edits limited to handoff/archive compaction; gitignored logs under `.agents/runs/20260702-233320-cron-qa-refresh/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored status/diff/CDP/test/typecheck/copy-scan logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected status/diff stat, prior handoff, coordination docs, `CLAUDE.md`, `STATUS.md`, and source/claims ledgers. CDP was reachable; sanitized tab inventory still showed GTO Wizard `/solutions`, RegLife lesson, PokerTrainer, and GTO login targets. Read-only structural CDP probe returned GTO Wizard `/solutions` counts only (`535` `data-tst`, `9` buttons, `2` roles); RegLife/PokerTrainer/login targets failed read-only Runtime inspection, so no private DOM content, answers, uploads, account, payment, or solver-output data was extracted. Because the branch still carries the accumulated external-review target-hints / Study Queue / SpotPacket / Data Health diff, this tick did not widen code scope and reran QA gates.
- Verification:           Focused Vitest passed (5 files / 38 tests, duration 29.75s); `npx tsc -b --pretty false` passed; strict changed-src copy scan found 0 RegLife/GamePlan/D#/dossier/payment/public-share hits in changed `src/` files. Initial post-handoff `agentKernel` exposed the handoff-size budget (5813 > 5120); after archiving the prior baton, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs saved under `.agents/runs/20260702-233320-cron-qa-refresh/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run increases targeted QA confidence only; it does not replace human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, avoid widening code scope; rerun verification only if the diff changes.

## 2026-07-02 - QA refresh on dirty Study Queue/SpotPacket diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edit limited to this handoff; gitignored logs under `.agents/runs/20260702-230240-cron-qa-handoff/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored status/diff/CDP/test/typecheck logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected status/diff stat, prior handoff, `CLAUDE.md`, protocols/board, `STATUS.md`, source/claims ledgers, and sanitized Chrome CDP inventory. CDP was reachable with GTO Wizard solution/login, RegLife lesson, PokerTrainer, Google search, iframe, and worker targets listed only by host/path/title; a read-only GTO Wizard `/solutions` structural probe returned selector/control counts only. Because the branch still carries the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff, this tick did not widen code scope and instead reran the targeted regression/typecheck gate.
- Verification:           Focused Vitest passed (5 files / 38 tests, duration 28.22s); `npx tsc -b --pretty false` passed; changed-src forbidden-copy scan found only route-fragment `d#` false positives and no actionable RegLife/payment/public-share additions. Initial post-handoff `agentKernel` exposed the handoff-size budget (5486 > 5120); after archiving the prior baton, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs saved under `.agents/runs/20260702-230240-cron-qa-handoff/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run increases targeted QA confidence only; it does not replace human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, avoid widening code scope; rerun verification only if the diff changes.

## 2026-07-02 - Build gate on Study Queue/SpotPacket diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edit limited to handoff documentation; gitignored build log under `.agents/runs/20260702-224000-cron-build-gate/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`; gitignored `npm-build.log`. Existing dirty app/product/research/archive files preserved.
- Summary:                Inspected `git status --short --branch`, diff stat/name inventory, `CLAUDE.md`, task/handoff protocols, two-agent board, `STATUS.md`, source/claims ledgers, and sanitized Chrome CDP tab inventory. Chrome CDP was reachable with GTO Wizard solution/login, RegLife lesson, PokerTrainer, Google search, iframe, and worker targets listed only by host/path/title. Took the prior baton’s next safe QA step and ran the production build; no private-site extraction or product code changes.
- Verification:           `npm run build` passed (exit 0; Vite built 3489 modules in 8.28s; PWA generated 65 precache entries; log `.agents/runs/20260702-224000-cron-build-gate/npm-build.log`). Initial post-handoff `agentKernel` exposed the handoff-size budget (5257 > 5120); after archiving the prior baton, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run increases production-build confidence only; it does not resolve the need for human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, avoid widening code scope; the next safe tick is a tiny source-ledger/local-selector audit or another focused verification pass only if the diff has changed.

## 2026-07-02 - Targeted gate on Study Queue/SpotPacket diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edit limited to this handoff; gitignored logs under `.agents/runs/20260702-223212-cron-targeted-gate/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored targeted-test/typecheck/status/diff logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected status/diff stat, prior handoff, `CLAUDE.md`, protocols/board, `STATUS.md`, source/claims ledgers, and sanitized Chrome CDP inventory. CDP was reachable with GTO Wizard solution/login, RegLife lesson, PokerTrainer, Google search, iframe, and worker targets listed only by host/path/title. Ran a focused regression/typecheck gate over the currently dirty StudyPlan, Study Queue route, StudyPlanCard, SpotSourcePanel, and HandReplay surfaces, then archived the prior baton to restore the handoff budget.
- Verification:           Focused Vitest passed (5 files / 38 tests, duration 27.92s); `npx tsc -b --pretty false` passed; changed-src forbidden-copy scan found no actionable RegLife/payment/public-share additions. Initial `agentKernel` exposed handoff size 5348 > 5120; after archive compaction, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs saved under `.agents/runs/20260702-223212-cron-targeted-gate/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run increases targeted QA confidence only; it does not replace human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, avoid widening code scope; rerun verification only if the diff changes.

## 2026-07-02 - Full Vitest gate on Study Queue/SpotPacket diff

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edits limited to handoff/archive compaction; gitignored logs under `.agents/runs/20260702-213245-cron-full-test/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored `npm-test.log` and `post-handoff-checks.log`. Existing dirty app/product/research files preserved.
- Summary:                Inspected status/diff stat, handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and sanitized CDP tab inventory. Chrome CDP was reachable with GTO Wizard solution/login, RegLife lesson, PokerTrainer, Google search, iframe, and worker targets listed only by host/path/title. Took the prior baton’s next safe QA step and ran the full Vitest suite; no private-site extraction or product code changes.
- Verification:           `npm test` passed: 78 files / 844 tests (exit 0; log `.agents/runs/20260702-213245-cron-full-test/npm-test.log`). Initial post-handoff `agentKernel` exposed the handoff-size budget (5257 > 5120); after archiving the prior baton, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run increases verification confidence only; it does not resolve the need for human review/commit. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, avoid widening code scope; the next safe tick is either `npm run build` if time budget allows or a tiny source-ledger/local-selector audit that does not touch active product files.

## 2026-07-02 - Study Queue/SpotPacket typecheck gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA/typecheck-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edits limited to handoff/archive compaction; gitignored log under `.agents/runs/20260702-180000-cron-typecheck-gate/`. No cron/Git/login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored `tsc.log`. Existing dirty app/product/research files preserved.
- Summary:                Inspected status, diff names/stat, handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, and sanitized CDP tab inventory. CDP was reachable with GTO Wizard solution, RegLife lesson, PokerTrainer, and GTO login tabs listed by sanitized host/path/title only. No product/private-site extraction changes; filled the missing TypeScript gate and archived the prior baton to restore the kernel handoff budget.
- Verification:           `npx tsc -b --pretty false` passed (exit 0; log `.agents/runs/20260702-180000-cron-typecheck-gate/tsc.log`). Initial post-handoff `agentKernel` exposed the handoff-size budget (5573 > 5120); after archive compaction, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run only increased verification confidence and handoff freshness; it did not widen product surface or inspect private result payloads. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, keep to tiny QA/source-ledger-only/local-selector slices; the next safe QA step is full `npm test` only if time budget is acceptable and no overlapping agent has taken ownership.

## 2026-07-02 - Study Queue/SpotPacket build gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA/build-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edits limited to this handoff plus archive compaction; gitignored logs under `.agents/runs/20260702-173240-cron-build-gate/`. No cron changes, Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored focused-test/build/docs/diff/kernel logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected `git status`, diff stat/name list, current handoff/archive, protocols, board, `CLAUDE.md`, `STATUS.md`, source/claims ledgers, modified StudyPlan/StudyPlanCard/SpotSourcePanel diffs, and sanitized CDP tab inventory. CDP was reachable with GTO Wizard solution, RegLife lesson, PokerTrainer, and GTO login tabs listed by sanitized host/path/title only. No new private-site extraction was attempted because the accumulated code diff is awaiting review. Added no implementation changes; this tick broadened verification by running the production build gate.
- Verification:           Focused Vitest passed (5 files / 38 tests). `npm run build` passed (Vite built 3489 modules and generated PWA assets). `npm run docs:check` and `git diff --check` passed. Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs are in `.agents/runs/20260702-173240-cron-build-gate/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run only increased verification confidence and handoff freshness; it did not widen product surface or inspect private result payloads. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, keep to tiny QA, source-ledger-only, or local-selector testability slices; do not add broad product surface until the dirty code diff is reviewed.

## 2026-07-02 - Focused Study Queue/SpotPacket QA gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edit limited to this handoff entry; gitignored logs under `.agents/runs/20260702-170209-cron-focused-qa/`. No cron changes, Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored focused-test/typecheck/docs/diff/kernel logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected `git status`, diff stat/name list, current handoff, protocols, board, `CLAUDE.md`, `STATUS.md`, claims ledger, research inventory, and sanitized CDP tab inventory. CDP remained reachable with GTO Wizard solution, RegLife lesson, and PokerTrainer tabs visible, but this slice intentionally avoided new private-site extraction because the accumulated code diff is awaiting review. Ran a focused QA gate over the modified Study Queue, StudyPlan, SpotSourcePanel, and HandReplay surfaces without changing implementation code, then archived the prior baton to restore the kernel handoff budget.
- Verification:           Focused Vitest passed (5 files / 38 tests). `npx tsc -b --pretty false` passed. `npm run docs:check` and `git diff --check` passed. Initial `agentKernel` exposed the handoff-size budget (5929 > 5120); after archive compaction, final `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests). Logs are in `.agents/runs/20260702-170209-cron-focused-qa/`.
- Risks / assumptions:    Branch remains intentionally dirty with the accumulated external-review target hints / Study Queue / SpotPacket / Data Health diff from prior ticks. This run only increased verification confidence and handoff freshness; it did not widen product surface or inspect private result payloads. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, keep to tiny QA, source-ledger-only, or local-selector testability slices; do not add broad product surface until the dirty code diff is reviewed.

## 2026-07-02 - Full test gate and sanitized CDP structural refresh

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  QA-only cron continuation on the already-dirty Study Queue/SpotPacket/Data Health diff. Tracked edits limited to this handoff plus archive compaction; gitignored logs/artifacts under `.agents/runs/2026-07-02-150157-cron-full-test-gate/`. Read-only sanitized CDP only; no cron changes, Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifact for npm test, tsc, build, CDP probe, docs/diff/kernel checks. Existing dirty app/product/research files preserved.
- Summary:                Inspected `git status`, handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, source/claim ledgers, and CDP tab inventory. Read-only CDP structural probe confirmed GTO Wizard `/solutions` remains inspectable with 535 `data-tst` attributes plus Study/Practice/Analyze/Upload/Hands/Reports/Ranges/History/Reset/Saved/action terms; RegLife lesson and PokerTrainer tabs timed out; GTO login tab was listed only and skipped. No implementation changes were made; prior active baton was archived to restore the kernel budget.
- Verification:           Full `npm test` passed (78 files / 844 tests). `npx tsc -b --pretty false` passed. `npm run build` passed. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed after this handoff update. Logs are in `.agents/runs/2026-07-02-150157-cron-full-test-gate/`.
- Risks / assumptions:    The branch remains intentionally dirty with the accumulated Study Queue/SpotPacket/Data Health diff from prior ticks; this run increased verification confidence rather than widening product surface. CDP artifacts retain sanitized host/path/title/count/term metadata only; no screenshots, storage changes, uploads, account data, solver ranges/frequencies, trainer answers, raw URLs, or private result payloads were kept. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, keep to tiny QA, source-ledger-only, or local-selector testability slices; do not add broad product surface until the dirty code diff is reviewed.

## 2026-07-02 - Verification-only CDP refresh and full gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification-only cron continuation on the dirty accumulated Study Queue/SpotPacket/Data Health diff. Tracked edits limited to this handoff plus archive compaction; gitignored logs/artifacts under `.agents/runs/2026-07-02-143156-cron-cdp-verification/`. Read-only sanitized CDP only; no cron changes, Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored CDP script/json/log plus focused Vitest, tsc, build, docs/diff/kernel logs. Existing dirty app/product/research files preserved.
- Summary:                Inspected `git status`, handoff/archive, protocols, board, `CLAUDE.md`, `STATUS.md`, and source/claim ledgers. Re-ran a sanitized CDP probe: GTO Wizard `/solutions` remained inspectable with 535 `data-tst` attributes and Study/Practice/Analyze/Upload/Hands/Reports/Ranges/History/Reset/Saved/action terms; RegLife lesson and PokerTrainer timed out; GTO login was listed only and skipped. No implementation changes were made.
- Verification:           Focused Study Queue/Data Health Vitest passed (5 files / 38 tests; existing jsdom `scrollTo` notice). `npx tsc -b --pretty false` passed. `npm run build` passed. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed after this handoff update. Logs are in `.agents/runs/2026-07-02-143156-cron-cdp-verification/`.
- Risks / assumptions:    This tick intentionally avoided widening feature surface because the worktree already contains a broad uncommitted Study Queue/SpotPacket/Data Health diff awaiting human review. CDP artifacts retain only sanitized host/path/title/count/term metadata; no screenshots, storage changes, uploads, account data, solver ranges/frequencies, trainer answers, raw URLs, or private result payloads were kept. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated diff. If cron continues before review, keep to tiny verification/QA or source-ledger-only slices; do not add broad product surface until the dirty code diff is reviewed.

## 2026-07-02 - Verification-only CDP and Study Queue guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Verification-only cron continuation after a dirty accumulated Study Queue/SpotPacket/Data Health diff. Tracked edits limited to handoff/archive compaction; gitignored `.agents/runs/2026-07-02-cron-verification-cdp/` logs/artifacts. Read-only sanitized CDP only; no login/account/payment/upload/trainer-answer/solver-frequency/range/storage mutation or raw private/tokenized URL retention.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored `cdp_sanitized_probe.py/json`, `focused-vitest.log`, `tsc.log`, `build.log`, final docs/diff/agent-kernel logs. Existing dirty app/research/status files preserved.
- Summary:                Inspected `git status`, current handoff, board, STATUS, CLAUDE, research ledgers, and Chrome CDP. CDP found GTO Wizard `/solutions`, RegLife lesson, PokerTrainer, and GTO login; login was listed only, GTO `/solutions` returned structural counts/term hits (`data-tst` 535; Study/Practice/Analyze/Upload/Hands/Reports/Ranges/History/Reset/Saved/action terms), and RegLife/PokerTrainer timed out under read-only WebSocket inspection. Reran focused Study Queue/Data Health guards without implementation changes, then archived the prior baton to restore handoff budget.
- Verification:           Logs/artifact in `.agents/runs/2026-07-02-cron-verification-cdp/`. Focused Vitest passed: 5 files / 38 tests, with existing jsdom `scrollTo` notice. `npx tsc -b --pretty false`, `npm run build`, `npm run docs:check`, and `git diff --check` passed. Initial `agentKernel` exposed the handoff budget (6906 > 5120); after archiving the prior baton, final `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    This tick intentionally avoided new feature work because the worktree already contains a broad uncommitted Study Queue/SpotPacket/Data Health diff and the prior baton requested human review or tiny verification-only guards. The CDP artifact stores sanitized host/path/title/structural counts only after title URL query/hash sanitization; one earlier terminal print briefly exposed a YouTube iframe title before the artifact script was corrected, but the retained artifact no longer contains that query. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Human review/commit of the accumulated diff is still preferred. If autonomous continuation proceeds before review, keep to small verification/QA guards or source-ledger-only research notes; avoid widening product surface until the dirty code diff is reviewed.

## 2026-07-02 - Data Health source-context overflow summary guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Small continuation on existing Study Queue/Data Health caveat-overflow work: `src/analysis/studyPlan.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `docs/product/STATUS.md`, handoff/archive. Read-only sanitized CDP only; no cron/Git mutation, account/payment/upload/trainer-answer/solver-output/raw-private/token action.
- Files touched:          `src/analysis/studyPlan.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `docs/product/STATUS.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifact in `.agents/runs/2026-07-02-datahealth-overflow-summary/`. Prior dirty app/research files preserved.
- Summary:                CDP probe stayed sanitized: GTO Wizard `/solutions` returned structural selector/control counts; Google search was listed; RegLife lesson, PokerTrainer, and GTO-login targets timed out, with no screenshots/storage/account data/uploads/trainer answers/solver frequencies/raw URLs. Implemented the prior requested guard: Data Health `sourceReasonSummary` now prioritizes concrete payout/player-field/PKO reasons on count ties and appends `+N more` when the eight-reason summary overflows. Added a regression for a legacy PKO final-table all-in spot with nine source/context reasons. Ran `npm run docs:update` for the new test count and archived the previous baton to restore the handoff budget.
- Verification:           Logs in `.agents/runs/2026-07-02-datahealth-overflow-summary/`. First narrow `studyPlan` Vitest failed once, proving alphabetical ordering hid `players remaining missing`; after priority ordering, `studyPlan` passed (7 tests). Focused Vitest passed: `studyPlan`, `StudyPlanCard`, `SpotSourcePanel`, `HandReplay`, and `studyQueueRouteContract` (5 files / 38 tests; existing jsdom `scrollTo` notice). `npx tsc -b --pretty false` passed; `npm run build` passed; initial `docs:check` caught stale STATUS and `docs:update` refreshed it. After handoff compaction, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    Ordering/summary/test guard only. It does not add solver EV, frequencies, trainer answers/scoring, account/upload flows, browser storage/progress mutation, payout/bounty values, parser imports, warning generation, cron jobs, commits, or Git history changes. RegLife remains licensed-private/brand-neutral; GTO Wizard remains private/tool workflow abstraction only.
- Next action requested:  Prefer human review/commit of the accumulated Study Queue/SpotPacket/Data Health caveat diff. If continuing autonomously before review, stay to tiny verification-only guards and avoid broad new feature surface.

## 2026-07-02 - Caveat overflow diff review and full gate

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Skeptical review/verification of the accumulated Study Queue/SpotPacket/Data Health caveat-overflow diff; read-only sanitized CDP structural probe only; no cron changes, commit/reset/delete/push, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-caveat-overflow-review/`, and ignored `dist/` output was regenerated by `npm run build`. Existing dirty app/product/research files from prior slices remain preserved.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, product/research ledgers, the current StudyPlan/StudyPlanCard/SpotSourcePanel/HandReplay/route-contract diffs, and sanitized Chrome/CDP state. CDP was reachable: GTO Wizard `/solutions` returned sanitized structural counts (no solver values/ranges/frequencies), Google search returned public structural counts, and RegLife/PokerTrainer/GTO-login targets timed out under read-only Runtime inspection; no screenshots, storage, uploads, account flows, raw URLs, account data, or trainer-answer actions were attempted. Reviewed the accumulated caveat UI: StudyPlan de-dupes per-hand source-context reasons, Dashboard shows a short queued-caveat summary with expandable full local details, and SpotSourcePanel prioritizes concrete payout/player/field caveats while exposing hidden overflow labels. Searched changed source for gate-risk copy; hits were no-EV/no-answer guard assertions or existing private/local wording, not new commercial/public-share/Reg Life-branded UI.
- Verification:           Logs in `.agents/runs/2026-07-02-caveat-overflow-review/`: `cdp_structural_probe.json`; focused `npx vitest run src/analysis/__tests__/studyPlan.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (5 files / 37 tests, existing jsdom `scrollTo` notice); `npx tsc -b --pretty false` passed; full `npm test -- --run` passed (78 files / 843 tests, existing jsdom `scrollTo`, localStorage experimental, and rangeChecker warning-test notices); `npm run build` passed; `npm run docs:check` passed before and after this handoff update; final `git diff --check` passed; final `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    Review/handoff-only slice; no runtime code changed in this tick. It does not change warning generation, StudyPlan ranking, SpotPacket schemas/hashes, parser imports, payout/field/bounty values, solver EV/frequencies, trainer answers/scoring, browser storage/progress semantics, account/payment/upload flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history. Existing accumulated Study Queue/SpotPacket/Data Health implementation and prior product/research docs remain preserved.
- Next action requested:  If this uncommitted diff remains, avoid broad new feature surface; either perform a final human-review-oriented diff summary or, if continuing autonomously, add only a tiny non-overlapping guard such as a Data Health reason-summary overflow-count assertion. Otherwise the accumulated caveat/export boundary work is ready for human review/commit.

## 2026-07-02 - Dashboard Data Health caveat details

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Make the dashboard Study Queue/Data Health queued-caveat summary locally expandable; read-only sanitized CDP structural probe only; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/components/dashboard/StudyPlanCard.tsx`, `src/components/dashboard/__tests__/StudyPlanCard.test.tsx`, `docs/product/STATUS.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/research/SOURCE_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-studyqueue-datahealth-caveat-details/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, product status, research ledgers, `StudyPlanCard`, dashboard and route-contract tests, and sanitized Chrome/CDP state. CDP stayed reachable: GTO Wizard `/solutions` returned sanitized structural selector/control counts (535 stable attributes), while RegLife, PokerTrainer, and GTO login targets timed out under read-only Runtime inspection; no screenshots, storage, uploads, account flows, solver ranges/frequencies, or trainer-answer actions were attempted. Replaced the dashboard Data Health caveat line clamp with an expandable details block: the summary shows the first two concrete source-context reasons plus `+N more`, and the local details list keeps payout, players-remaining, paid-places, and full-field-stack caveats inspectable. Updated the focused component regression to click the summary and assert hidden caveats become reachable, while route-contract coverage remains no-EV/no-chipEV/no-answer/no-score. Updated STATUS plus research ledgers and archived the prior active baton to keep `AGENT_HANDOFF.md` under the kernel budget.
- Verification:           Logs in `.agents/runs/2026-07-02-studyqueue-datahealth-caveat-details/`: sanitized `cdp_structural_probe.json`; `npx vitest run src/components/dashboard/__tests__/StudyPlanCard.test.tsx --reporter=verbose` passed (1 file / 7 tests); `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (1 file / 4 tests, existing jsdom `scrollTo` notice); `npx tsc -b --pretty false` passed; `npm run build` passed; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed; full `npm test -- --run` passed (78 files / 843 tests, with existing jsdom `scrollTo`, localStorage experimental, and rangeChecker warning-test notices).
- Risks / assumptions:    UI/test/docs change only. It does not change warning generation, StudyPlan ranking, SpotPacket schemas/hashes beyond normal packet construction, parser imports, payout/field/bounty values, solver EV/frequencies, trainer answers/scoring, browser storage/progress semantics, account/payment/upload flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history. Existing accumulated Study Queue/SpotPacket/Data Health implementation and prior handoff/archive edits remain preserved.
- Next action requested:  Good next bounded slice: skeptically review the accumulated Study Queue/SpotPacket/Data Health diff for duplicated or overlong source-context warnings across Dashboard, Hand Replay, and SpotSourcePanel, then run a broader `npm test -- --run` gate if the cron budget allows.

## 2026-07-02 - SpotSourcePanel caveat overflow details

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Continue the prior SpotSourcePanel caveat hardening by making hidden overflow warnings locally inspectable; read-only sanitized CDP tab inventory only; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/components/hands/SpotSourcePanel.tsx`, `src/components/hands/__tests__/SpotSourcePanel.test.tsx`, `docs/product/STATUS.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/research/SOURCE_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-spot-source-overflow-details/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, product status, research ledgers, `SpotSourcePanel`, and the focused component test. Replaced the inert `+N more` caveat chip with a local expandable details block that lists lower-priority hidden warnings while keeping the first eight visible chips prioritized around concrete ICM/PKO review inputs. Updated the overflow regression to click the control and assert hidden ICM/no-solver/legal-action/external-tool caveats remain reachable without EV-loss/chipEV/correct-answer/trainer-score wording. Updated STATUS plus research ledgers, then archived the two previous active batons to keep `AGENT_HANDOFF.md` under the kernel context budget. CDP check recorded only sanitized tab origin/path/title metadata; no screenshots, storage, uploads, account flows, solver ranges/frequencies, or trainer-answer actions were attempted.
- Verification:           Logs in `.agents/runs/2026-07-02-spot-source-overflow-details/`: `cdp_tab_inventory.json` recorded sanitized Chrome tab inventory; `npx vitest run src/components/hands/__tests__/SpotSourcePanel.test.tsx --reporter=verbose` passed (1 file / 9 tests); `npx tsc -b --pretty false` passed; `npm run docs:check` passed; `git diff --check` passed; `npm run build` passed (Vite plugin timing notice only). Initial post-handoff `agentKernel` exposed the handoff-size budget; after archiving prior entries, final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    UI/test/docs change only. It does not change warning generation, StudyPlan ranking, SpotPacket schemas/hashes beyond normal packet construction, parser imports, payout/field/bounty values, solver EV/frequencies, trainer answers/scoring, browser storage/progress semantics, account/payment/upload flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history. Existing accumulated Study Queue/SpotPacket/Data Health implementation and prior handoff/archive edits remain preserved.
- Next action requested:  Good next bounded slice: apply the same “truncated summary must reveal full local caveat labels” pattern to the dashboard Study Queue/Data Health drawer if any source-context warning summary is still capped there, with a focused `StudyPlanCard` regression and no solver/trainer-answer claims.

## 2026-07-02 - SpotSourcePanel caveat overflow priority

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Continue the prior Data Health/SpotPacket hardening by making Hand Replay `SpotSourcePanel` warning overflow preserve concrete ICM/PKO review inputs; read-only sanitized CDP structural probe; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/components/hands/SpotSourcePanel.tsx`, `src/components/hands/__tests__/SpotSourcePanel.test.tsx`, `docs/product/STATUS.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/research/SOURCE_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-spot-source-caveat-priority/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `docs/product/STATUS.md`, source/claims ledgers, StudyPlan/StudyPlanCard/SpotSourcePanel tests/code, and sanitized Chrome/CDP state. Reordered `SpotSourcePanel` warning priority so concrete tournament-context gaps (`missing_payouts`, `missing_players_remaining`, `missing_paid_places`, `missing_field_stack_distribution`) and PKO opponent/coverage/pay-jump gaps stay visible before lower-value generic chips when only eight caveats can render. Added a regression that builds a PKO/all-in/HRC packet with more than eight warnings and asserts payout/players/paid/full-field caveats, `+N more`, and no EV-loss/chipEV/correct-answer/trainer-score wording. Updated STATUS plus research ledgers to record the overflow-priority behavior. CDP structural probe reached GTO Wizard `/solutions` using sanitized counts only (no solver ranges/frequencies), while RegLife, PokerTrainer, and GTO login tabs timed out; no screenshots, storage, uploads, account flows, or trainer-answer actions were attempted.
- Verification:           Logs in `.agents/runs/2026-07-02-spot-source-caveat-priority/`: `cdp_structural_probe.json` recorded sanitized host/path/count metadata; focused `npx vitest run src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (4 files / 31 tests, with existing jsdom `scrollTo` notice); `npx tsc -b --pretty false` passed; `npm run docs:update` refreshed STATUS test counts; `npm run docs:check` passed; `git diff --check` passed; `npm run build` passed; full `npm test` passed (78 files / 843 tests, with existing jsdom `scrollTo` and Node localStorage experimental notices plus the existing `rangeChecker` warning assertion).
- Risks / assumptions:    UI warning-priority/test/docs change only. It does not change warning generation, StudyPlan ranking, SpotPacket schemas/hashes beyond normal packet construction, parser imports, payout/field/bounty values, solver EV/frequencies, trainer answers/scoring, browser storage/progress semantics, account/payment/upload flows, raw hand histories, player names, tokenized URLs, cron jobs, commits, or Git history. Existing accumulated Study Queue/SpotPacket/Data Health implementation and prior handoff/archive edits remain preserved.
- Next action requested:  Good next bounded slice: make the `+N more` caveat chip in `SpotSourcePanel` expose the hidden warning labels locally (for example an expandable/accessible overflow list) so lower-priority multi-bounty/external-tool caveats are not permanently hidden, with a focused component test and no solver/trainer-answer claims.

## 2026-07-02 - Route-contract Data Health caveat guard

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Review and harden the accumulated Study Queue/SpotPacket route-contract diff without changing product behavior; read-only sanitized CDP readiness probe; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/__tests__/studyQueueRouteContract.test.tsx`, `docs/agents/AGENT_HANDOFF.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-route-datahealth-contract/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected current dirty branch state, the prior handoff/protocols/board, `CLAUDE.md`, `docs/product/STATUS.md`, research ledgers, Study Queue/Data Health code, SpotSourcePanel, HandReplay export-test diff, and sanitized Chrome/CDP tab state. Tightened the route-contract fixture so the dashboard Data Health item uses real `source_context` evidence rather than inheriting a tagged-decision fixture, then asserted the dashboard caveat panel exposes concrete payout/players-remaining reasons while staying free of EV-loss/chipEV/correct-answer/trainer-score copy. CDP tab inventory found GTO Wizard `/solutions`, RegLife lesson, and PokerTrainer tabs; WebSocket DOM evaluation was blocked by Chrome's remote-origin guard (403), so only sanitized inventory/error metadata was stored and no private DOM text, screenshots, solver values/frequencies, storage, uploads, account flows, or trainer answers were captured.
- Verification:           Logs in `.agents/runs/2026-07-02-route-datahealth-contract/`: `cdp_structural_probe.json` recorded sanitized tab inventory plus the Chrome 403 WebSocket blocker; focused `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx src/components/dashboard/__tests__/StudyPlanCard.test.tsx src/components/hands/__tests__/HandReplay.test.tsx --reporter=verbose` passed (3 files / 22 tests, with existing jsdom `scrollTo` not-implemented notice); `npx tsc -b --pretty false` passed; full `npm test` passed (78 files / 842 tests, with existing jsdom `scrollTo` and Node localStorage experimental notices); `npm run build` passed.
- Risks / assumptions:    Test-only hardening. It does not change StudyPlan behavior, parser imports, SpotPacket schemas, payout/field/bounty values, solver EV/frequencies, trainer answers/scoring, browser storage/progress semantics, account/payment/upload flows, raw hand histories, player names, tokenized URLs, cron jobs, or Git history. Existing uncommitted Study Queue/SpotPacket/Data Health implementation, product-status, source-ledger, claims-ledger, handoff/archive work remains preserved.
- Next action requested:  Good next bounded slice is either: (1) reattempt CDP structural probing only after Chrome is launched with a compatible `--remote-allow-origins` setting, or (2) add a small `SpotSourcePanel`/StudyPlan test that verifies the `missing_players_remaining` and `missing_paid_places` warnings remain visible when more than eight caveats compete for display.

## 2026-07-02 - Dashboard Data Health queued-caveat visibility

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Study Queue/Data Health UI visibility for concrete ICM/PKO missing-context reasons; read-only/sanitized CDP readiness probe; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/raw-private/token actions.
- Files touched:          `src/components/dashboard/StudyPlanCard.tsx`, `src/components/dashboard/__tests__/StudyPlanCard.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`; gitignored logs/artifacts under `.agents/runs/2026-07-02-studyqueue-datahealth-caveat-ui/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, product/research docs, Study Queue/Card code, current SpotPacket/route-contract diffs, and sanitized Chrome/CDP state. CDP was reachable; tab inventory included GTO Wizard `/solutions`, RegLife lesson, PokerTrainer, Google/YouTube, and GTO Wizard login surfaces. A read-only GTO Wizard `/solutions` structural probe succeeded and recorded only sanitized selector/control-family metadata in a gitignored artifact; no private DOM text, solver values/frequencies, screenshots, uploads, account/payment/login flows, storage, or trainer answers were used. `StudyPlanCard` now surfaces the Data Health queue item's concrete evidence-detail reason summary (for example payout table / players remaining / paid places / full field stack distribution missing) in the dashboard Data Health panel next to retained import warning categories, keeping the item unsupported/no-EV/no-trainer-scoring. Added a component regression and updated product/source/claim ledgers.
- Verification:           Logs in `.agents/runs/2026-07-02-studyqueue-datahealth-caveat-ui/`: sanitized `cdp_structural_probe.json` captured tab inventory plus GTO Wizard structural selector counts; focused `npx vitest run src/components/dashboard/__tests__/StudyPlanCard.test.tsx --reporter=verbose` passed (1 file / 7 tests); focused `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (1 file / 4 tests, with existing jsdom `scrollTo` not-implemented notice); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:check` passed. Initial post-handoff `agentKernel` exposed the handoff-size budget after adding this entry; archiving the prior baton restored the budget, and final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    UI-only visibility slice. It does not add payout/field/bounty values, calculate risk premium, attach solver EV/frequencies, change parser imports, range/compliance math, financial math, local study SRS semantics, browser storage/progress semantics, account/payment/upload flows, trainer progress, raw hand histories, player names, tokenized URLs, or cron jobs. Existing uncommitted SpotPacket external-review, Study Queue source-context, route-contract, Hand Replay export-test, product-status, source-ledger, claims-ledger, and handoff/archive work remains preserved.
- Next action requested:  Good next bounded slice: review the full accumulated Study Queue/SpotPacket diff for any overlong Data Health reason-summary copy or duplicated source-context warnings across Dashboard, Hand Replay, and SpotSourcePanel; if clean, run a broader `npm test -- --run` gate or add one small route-contract assertion that the dashboard Data Health caveat panel remains no-EV/no-answer/no-score.

## 2026-07-02 - Study Queue ICM source-context split

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Study Queue/Data Health source-context warning taxonomy; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/studyPlan.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-studyqueue-icm-source-context/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, SpotPacket/Study Queue source-context code/tests, and sanitized CDP tab inventory. CDP was reachable with GTO Wizard `/solutions`, RegLife lesson, PokerTrainer, Google/YouTube, and GTO Wizard login tabs present; this tick only listed sanitized origins/paths and did not inspect private DOM, submit trainer answers, upload hands, or touch account/login/payment flows. Replaced the broad non-PKO “ICM/bounty spot needs tournament summary or payout review” Study Queue reason with concrete review inputs: payout table missing, players remaining missing, paid places missing, and full field stack distribution missing. PKO/final-table source-context items now surface those ICM inputs alongside opponent bounty, PKO coverage, multi-bounty, and pay-jump gaps while staying Data Health / SpotPacket study-only. Updated focused tests plus product/source/claim ledgers.
- Verification:           Logs in `.agents/runs/2026-07-02-studyqueue-icm-source-context/`: focused `npx vitest run src/analysis/__tests__/studyPlan.test.ts --reporter=verbose` passed (1 file / 6 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:check` passed; full `npm test -- --run` passed (78 files / 842 tests); final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    This is a warning/copy/test hardening slice only. It does not add payout/bounty values, calculate exact risk premium, attach solver EV/frequencies, add trainer answers/scoring, change parser imports, range tables, financial math, browser storage/progress semantics, account/payment/upload flows, trainer progress, raw hand histories, player names, tokenized URLs, or cron jobs. Existing uncommitted Hand Replay / route-contract export-test work remains preserved.
- Next action requested:  Good next bounded slice: skeptically review the accumulated SpotPacket/Study Queue diff for overbroad Data Health queueing or long reason-summary copy, then either tighten reason priority/display or add a small SpotSourcePanel/StudyPlanCard UI assertion for the new payout/field-stack reason labels.

## 2026-07-02 - Route SpotPacket export contract assertion

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Study Queue route-contract export test; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/__tests__/studyQueueRouteContract.test.tsx`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-route-spotpacket-export-contract/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, HandReplay/SpotSourcePanel/SpotPacket/route-contract code and tests, and sanitized CDP tab inventory. CDP was reachable with GTO Wizard `/solutions`, RegLife lesson, PokerTrainer, Google/YouTube, and GTO Wizard login tabs present; this tick only listed sanitized origins/paths and did not inspect private DOM, submit trainer answers, upload hands, or touch account/login/payment flows. Extended the `/hands?panel=spot-packet&reviewHand=...` route contract so the real Hand Replay packet panel must expose suggested-only HRC/ICMIZER target hints for an ICM-sensitive local route hand, and clicking the routed JSON export must keep `externalReview.result` not-attached/not solver-backed, trainer scoring not included, and the JSON free of raw hand text, local paths/filenames, private seat labels, solver EV/frequency fields, trainer answers, answer buckets, and trainer-score fields.
- Verification:           Logs in `.agents/runs/2026-07-02-route-spotpacket-export-contract/`: focused `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed (1 file / 4 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:check` passed; full `npm test -- --run` passed (78 files / 842 tests); final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    This is a route-test/readiness hardening slice only. It did not change runtime code, parser imports, range tables, financial math, solver adapters/results, browser storage/progress semantics, account/payment/upload flows, trainer progress, raw hand histories, player names, tokenized URLs, cron jobs, or the existing prior uncommitted Hand Replay export-test slice. The export still carries setup metadata only; exact HRC/ICMIZER/GTO Wizard/Postflopizer conclusions require a user-run external review/result with correct model/tree/range/payout/bounty assumptions.
- Next action requested:  Good next bounded slice: pivot to the next source-context/warning taxonomy item (for example payout/field-stack context copy or source-aware SpotPacket warning coverage) or run a skeptical review of the accumulated SpotPacket/Study Queue diff before adding more feature surface.

## 2026-07-02 - Hand Replay export contract assertion

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Hand Replay selected-packet export test; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/components/hands/__tests__/HandReplay.test.tsx`, `docs/product/STATUS.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-handreplay-export-contract/`, and ignored `dist/` output was regenerated by `npm run build`.
- Summary:                Inspected clean branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, relevant HandReplay/SpotSourcePanel/SpotPacket tests and code, and sanitized CDP tab inventory. CDP was reachable with GTO Wizard, RegLife lesson, PokerTrainer, Google, YouTube, and login tabs present; this tick only listed sanitized origins/paths and did not inspect private DOM, submit trainer answers, upload hands, or touch account/login/payment flows. Added a direct Hand Replay export regression: clicking the selected-hand SpotPacket JSON download now asserts `externalReview.targetHints` includes the suggested-only GTO Wizard/Postflopizer postflop family, external results stay `not_attached`/not solver-backed, trainer scoring stays `not_included`, and the JSON excludes raw hand-history markers, local path/filename markers, villain names, solver EV/frequency fields, trainer answers, answer buckets, and trainer-score fields. `npm run docs:update` refreshed the autogenerated STATUS test-call count.
- Verification:           Logged in `.agents/runs/2026-07-02-handreplay-export-contract/verification.log`: focused `npx vitest run src/components/hands/__tests__/HandReplay.test.tsx --reporter=verbose` passed (1 file / 11 tests); `npx tsc -b --pretty false` passed; `npm run docs:update` completed; `npm run docs:check` passed; `npm run build` passed; `git diff --check` passed; `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests); full `npm test -- --run` passed (78 files / 842 tests).
- Risks / assumptions:    This is a test/readiness hardening slice only. It did not change runtime code, parser imports, range tables, financial math, solver adapters/results, browser storage/progress semantics, account/payment/upload flows, trainer progress, raw hand histories, player names, tokenized URLs, or cron jobs. The export still carries setup metadata only; exact GTO Wizard/Postflopizer/HRC/ICMIZER conclusions require a user-run external review/result with correct model/tree/range/payout/bounty assumptions.
- Next action requested:  Good next bounded slice: extend `src/__tests__/studyQueueRouteContract.test.tsx` with a similarly sanitized route-level assertion that `/hands?panel=spot-packet&reviewHand=...` exposes the selected-hand JSON download and target-hints boundary, or pivot to the next source-context/warning taxonomy item if route-level coverage is already sufficient.

## 2026-07-02 - Hand Replay selected-packet target hints

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Hand Replay / SpotSourcePanel external-review metadata; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/components/hands/HandReplay.tsx`, `src/components/hands/SpotSourcePanel.tsx`, `src/components/hands/__tests__/HandReplay.test.tsx`, `src/components/hands/__tests__/SpotSourcePanel.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs were written under `.agents/runs/2026-07-02-handreplay-target-hints/`, and ignored build output under `dist/` was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, HandReplay/SpotSourcePanel code and tests, and Chrome CDP. CDP was reachable; GTO Wizard `/solutions` returned sanitized structural counts/control labels/class-hint samples, while RegLife lesson/PokerTrainer/GTO login page targets timed out under read-only `Runtime.evaluate` and no login/account/upload/trainer-answer action was attempted. Hand Replay now builds selected-hand `SpotPacket`s with `externalReview: { enabled: true }`, and `SpotSourcePanel` renders per-hand suggested-only target-family hints (HRC/ICMIZER, GTO Wizard/Postflopizer, or generic preflop setup families) with explicit no-auto-upload/no-EV/no-frequency/no-trainer-answer copy. Updated focused component tests plus source/claim ledgers and `STATUS.md`; `npm run docs:update` refreshed the autogenerated test-call count, and the prior baton was archived to keep `AGENT_HANDOFF.md` under the kernel budget.
- Verification:           Focused `npx vitest run src/components/hands/__tests__/SpotSourcePanel.test.tsx src/components/hands/__tests__/HandReplay.test.tsx --reporter=verbose` passed (2 files / 18 tests); `npx tsc -b --pretty false` passed; `npm run docs:update` completed; `npm run docs:check` passed; `npm run build` passed; full `npm test -- --run` passed (78 files / 841 tests). Initial post-handoff `agentKernel` exposed the handoff-size budget, then archiving restored `AGENT_HANDOFF.md` under the 5120-byte kernel limit; final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Logs saved in `.agents/runs/2026-07-02-handreplay-target-hints/`.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This slice touched already-dirty/untracked HandReplay/SpotSourcePanel/research/docs areas in the active Hermes worktree, did not stage/commit/reset/delete/push, and did not change parser imports, range tables, financial math, solver adapters/results, browser storage/progress semantics, account/payment/upload flows, trainer progress, raw hand histories, player names, tokenized URLs, or cron jobs. Target-family hints are routing/setup metadata only; exact HRC/ICMIZER/GTO Wizard/Postflopizer conclusions still require a user-run external review/result and correct model/tree/range/payout/bounty assumptions.
- Next action requested:  Good next bounded slice: add a tiny Hand Replay export/readiness assertion or route-contract expectation that downloaded selected-hand SpotPacket JSON includes `externalReview.targetHints` and still excludes raw hand text, local paths, villain names, solver EV/frequencies, trainer answers, and scoring; avoid adding browser-runner dependencies or private-site automation unless the user explicitly asks.

## 2026-07-02 - Study Queue suggested external-review target families

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused SpotPacket / Study Queue external-review metadata; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/spotPacket.ts`, `src/analysis/__tests__/spotPacket.test.ts`, `src/components/dashboard/StudyPlanCard.tsx`, `src/components/dashboard/__tests__/StudyPlanCard.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored build output under `dist/` was regenerated by `npm run build`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, relevant SpotPacket/StudyPlanCard code/tests, and Chrome CDP. GTO Wizard `/solutions` returned sanitized structural counts/control labels/selector samples; RegLife lesson and PokerTrainer tabs timed out under read-only CDP, and the GTO login tab was skipped. Added `SpotPacket.externalReview.targetHints` plus bundle-level target hint aggregation so external-review packets can suggest HRC/ICMIZER for ICM/PKO/all-in preflop review, GTO Wizard/Postflopizer for postflop tree/line review, and GTO Wizard/HRC for generic preflop configuration review. `StudyPlanCard` renders those hints as suggested-only metadata while preserving the no-auto-upload/no-EV/no-trainer/raw-data boundary. Updated source/claim ledgers and `STATUS.md`; `npm run docs:update` refreshed the autogenerated test-call count, and the prior baton was moved into the July archive to keep `AGENT_HANDOFF.md` under the kernel budget.
- Verification:           Focused `npx vitest run src/analysis/__tests__/spotPacket.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx --reporter=verbose` passed (2 files / 24 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; initial `npm run docs:check` correctly failed on stale autogenerated `STATUS.md` counts, then `npm run docs:update` completed and final `npm run docs:check` passed; `git diff --check` passed; full `npm test -- --run` passed (78 files / 840 tests). Post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed after archiving the previous entry.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This slice touched already-dirty/untracked SpotPacket/StudyPlanCard/research/docs areas in the active Hermes worktree, did not stage/commit/reset/delete/push, and did not change parser imports, range tables, financial math, solver adapters/results, browser storage, account/payment/upload flows, trainer progress, raw hand histories, player names, or tokenized URLs. Target-family hints are routing/setup metadata only; exact HRC/ICMIZER/GTO Wizard/Postflopizer conclusions still require a user-run external review/result and correct model/tree/range/payout/bounty assumptions.
- Next action requested:  Good next bounded slice: extend the same suggested-only target-family metadata into `SpotSourcePanel` for selected Hand Replay packet exports, so single-hand review shows the same target hints as Study Queue bundles without adding auto-upload, solver result import, EV/frequencies, trainer answers/scoring, or private raw data.

## 2026-07-02 - Study Queue external-review bundle summary

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused Study Queue / SpotPacket external-review bundle summary; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/spotPacket.ts`, `src/analysis/__tests__/spotPacket.test.ts`, `src/components/dashboard/StudyPlanCard.tsx`, `src/components/dashboard/__tests__/StudyPlanCard.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-study-queue-external-review/`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, relevant SpotPacket/StudyPlanCard code/tests, and sanitized Chrome CDP. GTO Wizard `/solutions` returned structural counts/control labels only; RegLife/PokerTrainer/login tabs were not interacted with. Added `SpotPacketBundle.externalReview`, aggregating per-packet external-review setup into target label, checklist coverage, missing-assumption counts, and not-attached result status. Study Queue bundles now include a generic setup checklist by default (callers can explicitly disable it), and `StudyPlanCard` renders the same summary in the source/config drawer/export JSON while preserving `study_packet_only`, no solver EV, no frequencies, no trainer answers/scoring, no raw hand text, no local paths, and no villain names.
- Verification:           Focused rerun `npx vitest run src/analysis/__tests__/spotPacket.test.ts src/components/dashboard/__tests__/StudyPlanCard.test.tsx --reporter=verbose` passed (2 files / 23 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:check` passed; `git diff --check` passed; full `npm test -- --run` passed (78 files / 839 tests). Final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This slice touched already-dirty/untracked SpotPacket/StudyPlanCard/research/docs areas in the active Hermes worktree, did not stage/commit/reset/delete/push, and did not change parser imports, range tables, financial math, solver adapters/results, browser storage, account/payment/upload flows, trainer progress, raw hand histories, player names, or tokenized URLs. The bundle checklist is a setup/export aid only; exact PKO/ICM/postflop advice still requires values, full stacks/tree settings, model settings, and an attached external result before any solver-backed label can be used.
- Next action requested:  Good next bounded slice: add source-governed target-family hints for Study Queue packets (for example HRC/ICMIZER for ICM/PKO/all-in preflop review, GTO Wizard/Postflopizer for postflop review) as metadata/copy only, without auto-uploading, importing solver results, or changing `study_packet_only` labels.

## 2026-07-02 - SpotPacket external-review checklist

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused SpotPacket external-review checklist code/docs slice; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/spotPacket.ts`, `src/analysis/__tests__/spotPacket.test.ts`, `src/components/hands/SpotSourcePanel.tsx`, `src/components/hands/__tests__/SpotSourcePanel.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-external-review/`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, relevant SpotPacket/SpotSourcePanel code/tests, and sanitized Chrome CDP. GTO Wizard `/solutions` returned structural selector/control counts only; RegLife/PokerTrainer/GTO-login tabs timed out. Added optional `SpotPacket.externalReview` metadata for non-generic external targets (HRC/ICMIZER/GTO Wizard/Postflopizer/etc.): target label, present/missing model assumptions, not-attached result status, and reproducible `packetHash`. External-target packets add setup warnings but remain `study_packet_only`; no solver EV, frequencies, trainer answers, trainer scoring, raw hand text, or villain names are imported/exported. `SpotSourcePanel` now renders the checklist.
- Verification:           Focused `npx vitest run src/analysis/__tests__/spotPacket.test.ts src/components/hands/__tests__/SpotSourcePanel.test.tsx --reporter=verbose` passed (2 files / 23 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:update` refreshed STATUS to 78 test files / 800 approximate test calls; `npm test -- --run` passed (78 files / 839 tests); final post-handoff `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests).
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This slice touched already-dirty/untracked SpotPacket/research/docs areas in the active Hermes worktree, did not stage/commit/reset/delete/push, and did not change parser imports, range tables, financial math, solver adapters/results, browser storage, account/payment/upload flows, trainer progress, raw hand histories, player names, or tokenized URLs. The checklist is a setup/export aid only; exact PKO/ICM advice still requires values, full stacks, model settings, and an attached external result before any solver-backed label can be used.
- Next action requested:  Good next bounded slice: surface the same external-review checklist in the Study Queue bundle drawer/JSON summary (bundle-level target coverage and missing assumption counts), still without accepting solver results or changing `study_packet_only` labels.

## 2026-07-02 - SpotPacket payout/bounty context presence

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused SpotPacket context-presence code/docs slice; no cron/Git mutation, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/spotPacket.ts`, `src/analysis/__tests__/spotPacket.test.ts`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs under `.agents/runs/2026-07-02-context-presence/`.
- Summary:                Inspected dirty branch state, prior handoff/protocols/board, `CLAUDE.md`, `STATUS.md`, research ledgers, relevant SpotPacket/StudyPlan/SpotSourcePanel code/tests, and sanitized Chrome CDP. GTO Wizard `/solutions` returned structural counts/selector samples only; RegLife/PokerTrainer/GTO-login tabs timed out. Added sanitized `payoutTable` and `opponentBountyTable` availability/source metadata to `SpotPacketTournament`; payout/pay-jump warnings now clear only when payout context exists, and PKO opponent-bounty/missing-bounty warnings clear only when opponent bounty-table context exists. Packets still remain `study_packet_only`, with opponent bounty values, solver EV, trainer answers, and trainer scoring absent. Updated STATUS/source/claim ledgers and archived the previous baton to restore the agent-kernel handoff budget.
- Verification:           Focused `npx vitest run src/analysis/__tests__/spotPacket.test.ts src/analysis/__tests__/studyPlan.test.ts src/components/hands/__tests__/SpotSourcePanel.test.tsx --reporter=verbose` passed (3 files / 27 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; `npm run docs:update` refreshed STATUS test-call count to 798; `npm run docs:check` passed; `git diff --check` passed. A pre-compaction full suite failed only on old handoff size (6521 > 5120); after archiving, `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (5 tests) and `npm test -- --run` passed (78 files / 837 tests). Logs: `.agents/runs/2026-07-02-context-presence/verification.log`.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This slice touched already-dirty/untracked research/code areas in the active Hermes worktree, did not stage/commit/reset/delete/push, and did not change parser imports, range tables, financial math, solver adapters, browser storage, account/payment/upload flows, trainer progress, raw hand histories, player names, or tokenized URLs. Context-presence metadata reduces missing-context warnings only; exact PKO/ICM advice still requires values, full stacks, model settings, and external validation.
- Next action requested:  Good next bounded slice: add an optional sanitized external-review target/config block for HRC/ICMIZER/GTO Wizard exports (target label, model assumptions present/missing, reproducible packet hash), without importing solver results or enabling `solver_backed_result` until an actual external output is attached.

## 2026-07-02 - PKO SpotPacket missing-context warnings

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Focused code/docs continuation from the prior PKO taxonomy baton; no cron/Git mutations, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `src/analysis/spotPacket.ts`, `src/analysis/__tests__/spotPacket.test.ts`, `src/analysis/studyPlan.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `src/components/hands/SpotSourcePanel.tsx`, `src/components/hands/__tests__/SpotSourcePanel.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`; verification log in `.agents/runs/2026-07-02-pko-missing-context-taxonomy/verification.md`.
- Summary:                Inspected dirty branch state, prior handoff, CLAUDE/board/task protocol, research ledgers, PKO taxonomy note, SpotPacket/StudyPlan/SpotSourcePanel sources, and CDP readiness (`127.0.0.1:9222` reachable; no private tabs or URLs inspected this tick). Added PKO-specific `SpotPacketWarning`s for unknown opponent bounty values, partial coverage context, multi-bounty all-in setup gaps, and PKO/pay-jump missing context; changed null tournament bounty to count as missing bounty context; mirrored concrete PKO reasons into the Data Health source-context Study Queue item; surfaced the labels in `SpotSourcePanel`; updated STATUS plus source/claims ledgers to mark the warning-taxonomy slice as implemented while preserving `study_packet_only` / no-EV / no-answer / no-scoring posture.
- Verification:           `npx vitest run src/analysis/__tests__/spotPacket.test.ts src/analysis/__tests__/studyPlan.test.ts src/components/hands/__tests__/SpotSourcePanel.test.tsx --reporter=verbose` passed (3 files / 25 tests); `npx tsc -b --pretty false` passed; `npm run build` passed; first `npm run docs:check` reported stale STATUS autogen, then `npm run docs:update` refreshed STATUS and `npm run docs:check` passed; `git diff --check` passed; `npm test -- --run` passed (78 files / 835 tests). Logs summarized in `.agents/runs/2026-07-02-pko-missing-context-taxonomy/verification.md`.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This tick touched files that were already dirty/untracked in the active worktree, did not stage/commit/reset/delete/push, and did not change parser imports, financial math, solver adapters, credentials, browser storage, account/payment/upload flows, trainer progress, raw hand histories, player names, or tokenized URLs. The PKO labels are warning taxonomy only; exact bounty/ICM advice still requires external validation/context.
- Next action requested:  Good next bounded slice: add optional `SpotPacket` context fields for user/import-provided payout table and opponent bounty table presence (not values unless explicitly entered/imported), plus tests proving packets can clear specific missing-context warnings only when the corresponding sanitized context is supplied; keep solver-backed labels unavailable until an external result/config is attached.

## 2026-07-01 - PKO / ICM missing-context study taxonomy

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Docs-only research/taxonomy pass; no app-code edits, cron/Git mutations, login/account/payment/upload/trainer-answer/solver-output/screenshot/raw-private/token actions.
- Files touched:          `docs/research/poker-brain/2026-07-01-pko-icm-missing-context-study-taxonomy.md`, `docs/research/poker-brain/README.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`.
- Summary:                Inspected dirty branch state, prior handoff/board/protocol docs, CLAUDE/STATUS/ROADMAP, ICM/bounty/final-table KB, `bountyAnalyzer.ts`, `spotPacket.ts`, `studyPlan.ts`, ledgers, and Chrome CDP. GTO Wizard `/solutions` returned sanitized structural metadata; RegLife/PokerTrainer/login timed out. Added the PKO/ICM taxonomy note plus `S-INT-KB-003`, `S-INT-KB-004`, `S-INT-CODE-024`, `C-PB-PKO-001` through `C-PB-PKO-003`, and `C-UX-STUDY-012`; archived the prior baton to keep the handoff under the kernel budget.
- Verification:           After this handoff update: `npm run docs:check` passed; `git diff --check` passed; `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests). Logs are in `.agents/runs/2026-07-01-pko-icm-taxonomy/`.
- Risks / assumptions:    Broad pre-existing dirty/untracked app/research work remains preserved. This tick did not touch app code, parser/range/scenario/financial logic, dependencies, lockfiles, cron, browser storage, account/payment/upload flows, solver outputs, screenshots, trainer progress, raw hand histories, player names, or tokenized URLs. RegLife remains licensed-private brand-neutral; GTO Wizard remains private/tool workflow evidence only.
- Next action requested:  Good next bounded code slice: add PKO-specific SpotPacket warnings and Study Queue source-context reasons (`opponent_bounty_values_unknown`, `pko_coverage_context_partial`, `multi_bounty_context_missing`, `pko_pay_jump_context_missing`) with `spotPacket.test.ts`, `studyPlan.test.ts`, and `SpotSourcePanel.test.tsx` coverage, preserving `study_packet_only` / no-EV / no-answer copy.

## 2026-07-01 - CDP structural probe operational guardrail

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Docs-only research/toolchain readiness; no cron/Git commit/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions and no browser-runner dependency install.
- Files touched:          `docs/research/design/2026-07-01-cdp-structural-probe-operational-note.md`, `docs/research/design/README.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; sanitized local logs under `.agents/runs/2026-07-01-cdp-structural-probe/`.
- Summary:                Inspected git status, prior handoff, CLAUDE/STATUS/board/protocol docs, ledgers, and Chrome CDP. CDP was reachable; `suppress_origin=True` returned sanitized structural metadata for GTO Wizard `/solutions`, while RegLife lesson/PokerTrainer/GTO login tabs timed out under read-only evaluation. Added `S-AUTH-GTOW-010`, `C-UX-STUDY-011`, and the operational note.
- Verification:           `npm run docs:check`, `git diff --check`, `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose`, and `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` passed.
- Risks / assumptions:    Broad pre-existing dirty/untracked research/Study Queue/SpotPacket/refusal/import work preserved. No app code, dependencies, cron, browser storage, account/payment/upload flows, solver outputs, screenshots, trainer progress, or raw private content changed.
- Next action requested:  Do not keep spending ticks on frozen private tabs unless they newly respond; use the structural probe only when CDP evidence is needed, then fall back to repo-visible work.

## 2026-07-01 - Browser-runner readiness guardrail

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Docs-only research/toolchain readiness; no cron/Git commit/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions and no browser-runner dependency install.
- Files touched:          `docs/research/design/2026-07-01-browser-runner-readiness.md`, `docs/research/design/README.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; sanitized local log under `.agents/runs/2026-07-01-browser-readiness/cdp_probe.json`.
- Summary:                Verified the previous browser-smoke fork before adding tooling. Chrome CDP is reachable and GTO Wizard `/solutions` remains inspectable, but RegLife/PokerTrainer/login targets still timed out under read-only CDP. Package/config/bin inventory found no Playwright/Cypress/Puppeteer/WebDriver runner, no e2e/spec config, and no local runner bins, so browser QA should be a separate dependency/tooling slice rather than an implicit cron install. Added `S-AUTH-GTOW-009`, `S-INT-CODE-023`, `C-UX-STUDY-010`, and the design readiness note.
- Verification:           `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed after archiving the previous baton to restore the kernel budget. Readiness also inspected git status, protocols/board/CLAUDE/STATUS, ledgers, sanitized CDP tabs, and browser-runner inventory.
- Risks / assumptions:    Broad pre-existing dirty/untracked research/Study Queue/SpotPacket/refusal/import work remains preserved. Docs-only; no code/dependency/lockfile/parser/range/scenario/financial/solver/upload/trainer-scoring/private-site state changed. GTO Wizard remains private/tool workflow evidence only; RegLife remains licensed-private brand-neutral scope.
- Next action requested:  Keep `src/__tests__/studyQueueRouteContract.test.tsx` as the current local smoke gate. If the user wants real browser QA, scope a dedicated dependency-change branch for `@playwright/test` (or equivalent) with sanitized local/demo packet seeding and owned-selector assertions; do not automate private study-site tabs or install browser tooling implicitly from cron.

## 2026-07-01 - Route-contract real terminal anchors

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Tight local Study Queue route-contract/testability follow-up; no cron/Git commit/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions.
- Files touched:          `src/__tests__/studyQueueRouteContract.test.tsx`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/research/design/2026-07-01-stable-selector-crosswalk.md`, `docs/agents/AGENT_HANDOFF.md`; logs under `.agents/runs/2026-07-01-route-real-anchors/`.
- Summary:                Extended the self-contained Study Queue route smoke so the Hand Replay/Data Health terminal handoff no longer relies on mocked placeholders. The SpotPacket review route now renders and asserts real `hand-replay-dialog`, `hand-replay-study-packet-review`, `spot-source-panel`, and `trainer-spot-card` anchors using sanitized local seat/action data; the Data Health route now renders and asserts real `hands-upload-root`, `hands-upload-data-health-entry`, and `import-data-health-panel`/`#data-health`. Source/claim/design research docs now record `S-INT-CODE-022` and the updated `C-UX-STUDY-009` status.
- Verification:           Readiness inspected: `git status --short --branch`, active handoff/board/CLAUDE/source ledgers, and sanitized CDP tabs (Chrome reachable; GTO Wizard/RegLife/PokerTrainer tabs present, no private DOM extraction attempted). Focused route smoke passed: `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx --reporter=verbose` (1 file / 4 tests) and `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx src/pages/__tests__/ArenaPage.test.tsx --reporter=verbose` (2 files / 16 tests). `npx tsc -b --pretty false`, `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed after archiving the previous entry to restore the kernel budget.
- Risks / assumptions:    Broad pre-existing dirty/untracked research/Study Queue/SpotPacket/refusal/import work remains preserved. This slice changes a route-contract test and research docs only; it does not alter parser, range tables, scenario detection, financial math, real solver integration, external uploads, trainer scoring, or private-site state. GTO Wizard remains user-authorized private/tool workflow evidence only; RegLife remains licensed-private brand-neutral scope.
- Next action requested:  If browser-runner tooling becomes available, add a local-only browser smoke seeded with sanitized/demo packets and assert the same owned selectors; otherwise keep `studyQueueRouteContract.test.tsx` as the gate for Study Queue/Coach/Arena/Hand Replay/Data Health refactors and continue opportunistic read-only RegLife/PokerTrainer/GTO inspection only when existing authenticated tabs respond.

## 2026-07-01 - Hand Replay/Data Health selector anchors

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Small UI/test/docs follow-up from the stable-selector crosswalk; no cron/Git/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions.
- Files touched:          `src/components/hands/HandReplay.tsx`, `src/components/hands/HandsUpload.tsx`, `src/components/hands/__tests__/HandReplay.test.tsx`, `src/components/hands/__tests__/HandsUpload.test.tsx`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/research/design/2026-07-01-stable-selector-crosswalk.md`, `docs/product/STATUS.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; logs under `.agents/runs/2026-07-01-hand-upload-selector-*`.
- Summary:                Added product-owned stable selectors for the two terminal study-loop gaps identified in the prior crosswalk: Hand Replay now has `hand-replay-overlay`, `hand-replay-dialog`, and `hand-replay-study-packet-review`; HandsUpload now has `hands-upload-root`, `hands-upload-dropzone`, and `hands-upload-data-health-entry` while preserving `import-data-health-panel` and `#data-health`. Updated focused component assertions and source/claim/status docs to mark the anchor gap closed, and archived the previous handoff entry to keep the active baton within the agent-kernel budget.
- Verification:           `npm test` passed after archiving the previous handoff entry (78 files / 831 tests). Focused checks also passed: HandReplay/HandsUpload Vitest (2 files / 25 tests), route-contract + Arena Vitest (2 files / 16 tests), `npx tsc -b --pretty false`, `npm run docs:check`, `git diff --check`, `npm run build`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose`.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This slice only added test selectors and docs; it did not alter parser, range tables, scenario detection, leak math, financial math, solver integration, trainer scoring, imports, external uploads, or account/private-site state. GTO Wizard remains user-authorized private/tool workflow evidence only; RegLife remains licensed-private brand-neutral scope.
- Next action requested:  Use the owned selectors for a self-contained local browser/manual smoke seeded by sanitized local packets rather than private GTO/RegLife/PokerTrainer tabs; if browser automation is not available, extend the route-contract test to assert the real Hand Replay/Data Health anchors instead of only mocked terminal surfaces.

## 2026-07-01 - Stable selector crosswalk for study-loop QA

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Docs/research selector-instrumentation crosswalk plus read-only CDP readiness; no cron/Git/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions; no app source edits in this tick.
- Files touched:          `docs/research/design/2026-07-01-stable-selector-crosswalk.md`, `docs/research/design/README.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`; ignored `.agents/runs/2026-07-01-cdp-and-selector-inventory.*`.
- Summary:                Inspected dirty repo state, active handoff/protocols/board, `CLAUDE.md`, product status, research ledgers/design notes, and Chrome CDP. Ran a sanitized CDP/local-selector inventory: GTO Wizard `/solutions` was inspectable with broad workflow/test-attribute coverage; RegLife lesson, PokerTrainer, and GTO login targets timed out under read-only Runtime evaluation. The local inventory found strong owned selectors for Study Queue, Coach, Arena, Spot Source, and Trainer Prompt, with explicit Hand Replay/Data Health anchors as the next browser-smoke gap.
- Verification:           `npm run docs:check` passed. `git diff --check` passed. `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (5 tests) after archiving the prior handoff entry to restore the kernel budget. Logs stored under `.agents/runs/2026-07-01-stable-selector-*`.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This slice changed docs/research/handoff only; no parser, range tables, scenario detection, leak math, financial math, solver integration, trainer scoring, external upload, account state, screenshot, or local app source changed. GTO Wizard remains user-authorized private/tool workflow evidence only; RegLife remains licensed-private brand-neutral scope; no solver ranges/frequencies, trainer answers, account data, screenshots, storage, raw URLs, or raw hand histories were retained.
- Next action requested:  If no overlapping UI owner is active, add narrow product-owned `data-testid` anchors for Hand Replay packet review and Upload/Data Health entry, then extend focused component/route-contract tests. Otherwise keep repo-visible QA/docs moving and retry RegLife/PokerTrainer read-only CDP only when existing authenticated tabs respond.

## 2026-07-01 - Local study-loop route-contract execution

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Docs/research QA plus read-only CDP readiness and focused local route-contract verification; no cron/Git/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions.
- Files touched:          `docs/research/design/2026-07-01-local-study-loop-route-contract-execution.md`, `docs/research/design/README.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored `.agents/runs/2026-07-01-local-study-loop-*.log`.
- Summary:                Inspected dirty repo state, active handoff/protocols/board, `CLAUDE.md`, research ledgers/design notes, and Chrome CDP. CDP listed RegLife lesson, GTO Wizard Solutions, and PokerTrainer tabs; sanitized read-only Runtime metadata succeeded only for GTO Wizard `/solutions` (source/config selector, Study/Practice/Analyze/Upload navigation, reset/save/practice controls, action-tree/tab selector families), while RegLife lesson and PokerTrainer timed out under Runtime/Page/DOM/Accessibility commands. Added source/claim ledger rows `S-AUTH-GTOW-007`, `S-INT-CODE-019`, and `C-UX-STUDY-008`, plus a design QA execution note showing the local Study Queue → Coach → Arena → Hand Replay/Data Health smoke should be self-contained around sanitized packets rather than external private-tab responsiveness.
- Verification:           Focused `npx vitest run src/__tests__/studyQueueRouteContract.test.tsx src/pages/__tests__/ArenaPage.test.tsx --reporter=verbose` passed (2 files / 16 tests). `npm run docs:check` passed. `git diff --check` passed. `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed (1 file / 5 tests). Logs stored under `.agents/runs/2026-07-01-local-study-loop-*.log`.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This slice changed docs/research/handoff only; no parser, range tables, scenario detection, leak math, financial math, solver integration, trainer scoring, external upload, account state, or local app source changed. GTO Wizard remains user-authorized private/tool workflow evidence only; RegLife remains licensed-private brand-neutral scope; no solver ranges/frequencies, trainer answers, account data, screenshots, or raw tokenized URLs were retained.
- Next action requested:  If no overlapping UI owner is active, either run a local/demo browser smoke against the documented selectors or extend the existing Vitest route-contract around any remaining Study Queue/Coach/Arena/Hand Replay edge before refactoring. Retry RegLife/PokerTrainer read-only CDP only if existing authenticated tabs respond; otherwise keep repo-visible QA moving.

## 2026-07-01 - Cross-surface reference-copy cleanup

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Trust-copy only across review surfaces plus read-only CDP readiness; no cron/Git/login/account/payment/upload/trainer-answer/solver-value/screenshot/raw-private/token actions.
- Files touched:          `SessionsPage`, `CareerPage`, `ArenaPage`, `LeaksPage`, `HandsFilters`, `HandReplay`, `LifetimeScorecard`, `utils/evidence`, focused tests, `STATUS`, research ledgers, handoff + July archive; ignored `.agents/runs/2026-07-01-reference-copy-*.log`.
- Summary:                Inspected dirty repo state, active handoff/protocols/board, `CLAUDE.md`, STATUS/research ledgers, and Chrome CDP. CDP tab listing was reachable; read-only Runtime metadata for GTO Wizard `/solutions` returned sanitized workflow/testability shape (complete page, 499 stable data selectors, Study/Practice/Analyze/Upload/Strategy/Range/Breakdown/Reports/Hands labels), while RegLife lesson/PokerTrainer/login targets timed out or were skipped after sanitized metadata attempts. No external-site interactions, uploads, screenshots, solver values, trainer answers, account data, raw URLs, or private chart content were retained. Neutralized remaining high-visibility GTO/compliance-style copy in Session History, Career, Arena, Hand Archive filters, Hand Replay verdicts, Lifetime scorecard, Leak Inbox evidence/source labels, and evidence utility tooltips so local chart checks read as `reference`, `baseline`, or `rule/proxy` checks rather than solver-backed/absolute GTO output. Added source/claim ledger rows `S-INT-CODE-018` / `C-UX-COPY-001` and refreshed STATUS/docs inventory after adding a focused HandsFilters test.
- Verification:           `npm run docs:update` passed (STATUS now 78 test files / 792 approx tests). Focused Vitest passed (5 files / 33 tests): Arena, HandReplay, HandsFilters, LifetimeScorecard, evidence. `npx tsc -b --pretty false` passed. `npm run build` passed. Final `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` passed. Scoped source search found no legacy `GTO Comp`, `GTO Consistency`, `GTO Compliant`, `Standard GTO`, `GTO / Compliance`, or `GTO Compliance Rate` strings in production code; remaining scoped `GTO` matches are negative test assertions only.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This slice changes user-facing copy/tests/docs only; no parser, range tables, scenario detection, leak math, financial math, solver integration, or trainer scoring changed. Some generic `compliance` terminology remains intentionally where it names existing filters/metrics and should be reviewed separately if the product wants a full terminology rename.
- Next action requested:  Run a local/demo browser smoke for Dashboard Study Queue → Coach → Arena → Hand Replay using in-repo selectors, or continue a narrow terminology pass on remaining generic `Compliance & Theory` / `theoretical range` comments in Ranges once no overlapping UI owner is active. Retry RegLife/PokerTrainer read-only CDP only if existing authenticated tabs respond; keep solver-backed, rule/proxy, reference, and study-packet-only labels separated.

## 2026-07-01 - Range Matrix reference-copy hardening

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Small Range Matrix trust-copy/test slice plus read-only CDP readiness. No cron edits, Git mutations, login/account/payment/upload actions, trainer-answer submission, solver-frequency extraction, screenshots, raw private content, raw hand histories, villain-name export, tokenized URLs, or secrets.
- Files touched:          `src/components/shared/DualRangeMatrix.tsx`, `src/components/shared/__tests__/DualRangeMatrix.test.tsx`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_07.md`; ignored logs `.agents/runs/2026-07-01-dual-range-reference-copy-vitest.log`, `.agents/runs/2026-07-01-dual-range-reference-copy-typecheck.log`, `.agents/runs/2026-07-01-dual-range-reference-copy-build.log`, `.agents/runs/2026-07-01-dual-range-reference-copy-final-checks.log`, `.agents/runs/2026-07-01-dual-range-reference-copy-post-handoff-final.log`.
- Summary:                Inspected dirty repo state, active board/protocols/handoff, `CLAUDE.md`, STATUS/research ledgers, and sanitized Chrome CDP. CDP was reachable; GTO Wizard `/solutions` read-only Runtime inspection returned workflow selector metadata only (455 stable selector names; Study/Practice/Analyze/Upload/Strategy/Range/Breakdown/Reports/Hands labels), while RegLife lesson and PokerTrainer targets timed out, so no private DOM extraction beyond sanitized metadata, screenshot, login/account/payment/upload, trainer interaction, solver values, or raw URL retention was attempted. Hardened `DualRangeMatrix` copy: Oracle now says `The Oracle (Reference)`, in-range cells say `Reference Range`, default pane says reference-vs-sample, and hand-level insight is framed as context-dependent review guidance instead of “GTO Standard,” “Always open,” standard-fold, or solver-EV claims. Added source/claim ledger rows `S-INT-CODE-017` / `C-UX-RANGE-002` and updated STATUS.
- Verification:           Focused Vitest passed: `npx vitest run src/components/shared/__tests__/DualRangeMatrix.test.tsx --reporter=verbose` (1 file / 5 tests). `npx tsc -b --pretty false` passed. `npm run build` passed. Final checks passed: `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` (5 tests). Scoped source search in `src/components/shared`, `src/pages/RangesPage.tsx`, and `src/analysis/rangeValidator.ts` found no remaining shippable `GTO`, `GTO Standard`, `Solver %`, `solver output`, RegLife, or HRC copy outside negative test assertions and explicit “not solver EV” boundary copy.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This slice changes display copy only; no range tables, parser logic, compliance math, financial math, or solver integration changed. Other user-facing GTO/compliance wording remains outside this slice (`SessionsPage`, `HandReplay`, `HandsFilters`, `CareerPage`, `ArenaPage`, `LeaksPage`, `LifetimeScorecard`, `utils/evidence`) and should be reviewed separately before claiming global GTO-copy neutralization.
- Next action requested:  Continue the skeptical privacy/IP/trust-copy pass on the remaining GTO/compliance labels listed above, or run a local/demo browser smoke for Dashboard Study Queue → Coach → Arena → Hand Replay once a runner is selected. Retry RegLife/PokerTrainer read-only CDP only if existing authenticated tabs respond; keep solver-backed, rule/proxy, reference, and study-packet-only labels separated.

## 2026-07-01 - Range trust copy neutralization

- Owner / agent:          Hermes
- Branch:                 hermes/worktree-20260627-213824
- Scope:                  Small range/evidence trust-copy slice plus read-only CDP readiness. No cron edits, Git mutations, login/account/payment/upload actions, trainer-answer submission, solver-frequency extraction, raw private content, raw hand histories, villain-name export, tokenized URLs, or secrets.
- Files touched:          `src/analysis/rangeValidator.ts`, `src/pages/RangesPage.tsx`, `src/pages/LeaksPage.tsx`, `src/analysis/studyPlan.ts`, `src/components/shared/RangeGrid.tsx`, `src/analysis/bountyAnalyzer.ts`, `src/analysis/__tests__/rangeValidator.test.ts`, `src/analysis/__tests__/studyPlan.test.ts`, `docs/knowledge/strategy/02-ranges-and-position.md`, `docs/product/STATUS.md`, `docs/research/SOURCE_LEDGER.md`, `docs/research/CLAIMS_LEDGER.md`, `docs/agents/AGENT_HANDOFF.md`, `docs/agents/archive/AGENT_HANDOFF_ARCHIVE_2026_06.md`; ignored logs `.agents/runs/2026-07-01-range-trust-copy-focused.log`, `.agents/runs/2026-07-01-range-trust-copy-typecheck.log`, `.agents/runs/2026-07-01-range-trust-copy-build.log`, `.agents/runs/2026-07-01-range-trust-copy-final-checks.log`.
- Summary:                Inspected dirty repo state, protocols/board/handoff, `CLAUDE.md`, shipped status, research ledgers, and sanitized Chrome CDP. CDP was reachable; GTO Wizard `/solutions` returned workflow/selector metadata only, while RegLife lesson and PokerTrainer tabs timed out, so no private DOM extraction, screenshot, login/account/payment/upload, or trainer interaction was attempted. Neutralized range-validation provenance copy: Ranges now says “Reference %” and documented chipEV reference baselines; range/study evidence quotes match the knowledge doc; source comments/tests no longer call local range tables GTO Wizard/HRC/solver output. Added `S-INT-CODE-016` / `C-UX-RANGE-001` and STATUS coverage.
- Verification:           Focused Vitest passed: `npx vitest run src/analysis/__tests__/rangeValidator.test.ts src/analysis/__tests__/studyPlan.test.ts src/pages/__tests__/LeaksPage.test.tsx --reporter=verbose` (3 files / 23 tests). `npx tsc -b --pretty false` passed. `npm run build` passed. Final checks passed: `npm run docs:check`, `git diff --check`, and `npx vitest run src/__tests__/agentKernel.test.ts --reporter=verbose` (5 tests). Follow-up source search found zero `src/` matches for the removed RegLife/GTO Wizard/HRC/solver-output phrasing query.
- Risks / assumptions:    Broad pre-existing dirty/untracked Study Queue/SpotPacket/refusal/import/research work remains preserved. This is a provenance/copy cleanup, not a math/range-table behavior change; internal property names like `solverPct` remain for compatibility. RangesPage copy has build/typecheck coverage but no dedicated UI test. GTO Wizard remains private/tool workflow abstraction only; RegLife/PokerTrainer were not responsive to read-only CDP this tick.
- Next action requested:  Continue the skeptical privacy/IP/copy review of the current dirty Study Queue/SpotPacket diff, or run a local/demo browser smoke for Dashboard Study Queue → Coach → Arena → Hand Replay once a runner is selected. Retry RegLife/PokerTrainer read-only CDP only if existing authenticated tabs respond; keep solver-backed, rule/proxy, reference, and study-packet-only labels separated.
