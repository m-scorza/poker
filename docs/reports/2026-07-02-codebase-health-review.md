---
status: open
date: 2026-07-02
related: ['#108', '#109', '#110', '#113']
---
# Codebase Health Review — 2026-07-02

**Mode:** read-only scheduled review; this report is the only file produced.
**Branch reviewed:** `claude/relaxed-mccarthy-986zq5` @ `39550c8` (identical to `origin/main`).
**Graphify:** unavailable in this environment (local-only tool since #65, lives on the
owner's machine) — the dependency map was rebuilt from source with an ad-hoc import-graph
scan (fan-in, cycles, orphans) plus direct inspection.

## Open items

- [ ] Flip `docs/reports/2026-06-12-principal-engineer-audit.md` (and its appendix) to
      `status: resolved` and `git mv` to `archive/` — its stated close condition
      ("stays open until the Act I correctness items close") was met when Act I
      (I-1..I-5, PRs #94–#101) and Act II (#102–#110) completed. The SessionStart hook
      re-surfaces it every session for no remaining reason. Residuals are already
      tracked in ROADMAP "Known residuals".
- [ ] Fix the **manual** "Routes actually wired" table at the top of
      `docs/product/STATUS.md` (~lines 28–42): it still says `/` → `DashboardPage`,
      has no `/dashboard` row, and describes `/villains` as "Auto-classified
      archetypes" — all contradicted by the **autogenerated** routes block in the
      same file (lines 328–343) and by `src/App.tsx`. Either delete the manual table
      (the autogen block is enforced and correct) or rewrite it. This is the
      "second source of truth drifts" failure mode from the 2026-06-12 review,
      recurring inside STATUS.md itself.
- [ ] Update `CLAUDE.md` for #108/#109: line 87 (Villain Tracker "Auto-classify
      opponents" — parked, stats-only now), line 377 (sidebar/front-door layout —
      Coach's Note is `/` now), line 393 (lists `StudyPlanCard`, `ValueSnapshotCard`
      under dashboard components), §"Villain Auto-Classification" (line 483 — mark
      as parked/dormant spec, not shipped behavior).
- [ ] Decide on `src/components/dashboard/StudyPlanCard.tsx` (256 L) and
      `ValueSnapshotCard.tsx` (~150 L): zero importers since at least #60
      (2026-06-12), zero tests, no dormant-banner (unlike `villainClassifier.ts`,
      which is explicitly parked). Delete, or mark dormant with a banner + owner
      intent.
- [ ] (Low) Untangle the type-layer inversion: `src/types/analysis.ts` imports types
      from `analysis/postflopAnalyzer.ts`, `analysis/bountyAnalyzer.ts`, and
      `analysis/finalTableAnalyzer.ts` — the last one forming the repo's only
      import cycle (type-only, runtime-safe, but `types/` should not depend on
      `analysis/`). Move `FakeShoveSpot`/`RestealSpot`/`PostflopAction`/
      `BountyContext` into `types/`.
- [ ] (Low) Migrate root-level `AUDIT_NEW.md` (28 KB legacy audit, predates the
      reports lifecycle, referenced as debris in the 2026-05 handoff archive) into
      `docs/reports/archive/` with frontmatter, or resolve and delete it.

## Verification gate (this run)

- `npm run docs:check` — pass.
- `npm run typecheck` + `typecheck:test` — pass.
- `npm run lint` — pass, 2 warnings (`jsx-a11y` at `HandsUpload.tsx:463`).
- `npm test` — **769 / 769 passing, 69 files**.
- `npm run build` — pass (3,484 modules).
- `npm audit` — 0 vulnerabilities.
- `grep TODO|FIXME|HACK src/` — 0 hits.

## Codebase Health Summary

- **Overall health: good.** Fully green CI locally, zero audit vulnerabilities, zero
  TODO debris, strong test coverage in the correctness-critical layers
  (parser/analysis/data), chip-conservation invariant in CI, clean working tree.
- **Main risks:** documentation drift reopening (STATUS manual table + CLAUDE.md
  contradict shipped #108/#109 behavior); a stale-open audit report numbing the
  session-start surfacing mechanism; large untested page components
  (`CareerPage` 640 L, `RangesPage` 525 L, `LeaksPage` 392 L, `VillainsPage` 358 L).
- **Highest-impact improvement:** one small docs PR that closes the 2026-06-12 audit,
  fixes the STATUS manual routes table, and updates the four stale CLAUDE.md spots.
- **Confidence:** high for everything above (all direct-inspection + local CI);
  none of it depends on Graphify.

## Dependency map (rebuilt from source)

- **Top fan-in:** `types/hand.ts` (52), `types/analysis.ts` (50),
  `analysis/leakDetector.ts` (19), `data/store.ts` (19), `parser/pokerstars.ts` (15),
  `data/strategyProfiles.ts` (15), `analysis/rangeChecker.ts` (14),
  `data/appStore.ts` (14).
- **Cycles:** exactly one, type-only (`types/analysis.ts` ↔
  `analysis/finalTableAnalyzer.ts`). The `importRuns ↔ store` runtime cycle from the
  2026-06-12 review is **resolved and verified** (fixed in #70).
- **Orphans:** all page "orphans" are false positives (lazy `import()` in `App.tsx`);
  `parser/worker.ts` is loaded via `new Worker(new URL(...))` in
  `HandsUpload.tsx:285`. The only true orphans are `StudyPlanCard.tsx` and
  `ValueSnapshotCard.tsx` (see open items).
- **Largest files:** `data/store.ts` 910 L (cohesive persistence facade — many small
  exported functions, not a god-object; acceptable), `HandsUpload.tsx` 859 L,
  `HandReplay.tsx` 692 L, `parser/pokerstars.ts` 677 L, `CareerPage.tsx` 640 L.

## Test-coverage distribution

Well covered: `analysis/` (25 test files), `parser/` (16, incl. fixture sweep with the
chip-conservation invariant), `data/` (10), shared components (7).
Gaps: only 2 of 12 pages have component tests (`ArenaPage`, `HandsPage`);
`data/appStore.ts`, `utils/csvExport.ts`, `utils/pdfExport.ts`, `utils/format.ts`
untested. Page logic mostly delegates to tested analysis modules, so severity is
moderate — `CareerPage` and `RangesPage` are the two carrying the most inline logic.

## Continuity vs previous reviews

- **Resolved since 2026-06-12 review (verified):** committed Graphify snapshot (#65);
  `importRuns↔store` cycle (#70); STATUS verification-stamp staleness (replaced by
  enforced autogen blocks, #74); vitest advisory (#62/#105); parser chip-accounting
  keystone A1/A2 (#94/#98, conservation now CI-enforced).
- **Recurring:** "second source of truth drifts" — new instance is STATUS.md's manual
  routes table vs its own autogen block; `AUDIT_NEW.md` root debris (noted in the
  2026-05 handoff archive, still present).
- **New:** stale-open principal audit; CLAUDE.md drift from #108/#109; dead dashboard
  cards; type-layer inversion.
- **Watchlist (not bugs):** GGPoker parser is integer-chips-only by design
  (`RE_UNCALLED` has a `\$?` but no decimal support — consistent with the documented
  "scaffold" scope; would silently mis-account if decimal cash-game GG files are ever
  imported); 2 `jsx-a11y` warnings at `HandsUpload.tsx:463`.

## Review Ledger

- **Date/time:** 2026-07-02 (scheduled run)
- **Trigger:** scheduled codebase-health routine
- **Branch:** `claude/relaxed-mccarthy-986zq5`
- **Commit:** `39550c8` (== `origin/main`)
- **Scope:** full repo — git state, dependency graph rebuild, CI gate, docs
  cross-check, dead-code/orphan sweep, coverage mapping, security audit
- **Graphify sync status:** absent in this environment (local-only since #65);
  substituted with source-derived graph — no Graphify-derived claims made
- **Files changed since last run (2026-06-12 review):** PRs #65–#113 (Act I
  correctness epics, Act II coach loop, dashboard demotion, archetype parking,
  Vite 8/Vitest 4 upgrades, dep advisories)
- **Areas inspected:** `App.tsx` routes, `types/analysis.ts`,
  `analysis/finalTableAnalyzer.ts`, `data/store.ts`, `data/appStore.ts`,
  `parser/ggpoker.ts`, `parser/pokerstars.ts`, `components/dashboard/*`,
  `HandsUpload.tsx`, `STATUS.md`, `ROADMAP.md`, `CLAUDE.md`, reports index +
  frontmatter, full local CI gate
- **New findings:** 4 (stale-open audit, CLAUDE.md drift, dead cards, type inversion)
- **Recurring findings:** 2 (STATUS second-source drift pattern, AUDIT_NEW.md debris)
- **Resolved findings:** 5 (see continuity section)
- **Stale findings:** none carried without re-verification
- **Recommended next actions:** see Open items above, in order
