---
status: open
date: 2026-07-03
related: ['docs/reports/2026-07-01-abyss-audit.md', '#117', '#118', '#120', '#121']
---

# Codebase Health Review — 2026-07-03

Scheduled Graphify-assisted health review of `main` @ `9c4621e` (after
PRs #116–#122, i.e. abyss Wave 0 + Hermes salvage R1–R5), performed in a
remote session on branch `claude/relaxed-mccarthy-x2kkmv`. **Graphify was
unavailable in this environment** (see Sync Status below); the navigation
map was rebuilt locally with madge, an import fan-in sweep, LOC metrics,
and the production bundle, and every finding was verified by direct
inspection or by running the verification gate.

## Open items

New findings only — everything recurring is tracked by the abyss-audit
waves and is *not* duplicated here (statuses re-verified below).

- [ ] **Wire or remove the dead study-queue bundle + SRS routing engine**
      (~380 lines, zero production consumers): `buildStudyQueueSpotPacketBundle`
      (`spotPacket.ts:1177` + bundle types/helpers) and
      `selectNextStudyPacket` / `selectNextActionableStudyPacket` /
      `buildStudyPacketArenaSession` / `buildStudyPacketArenaPath` /
      `studyPacketSrsStatusLabel` (`studyPacketProgress.ts:98–217`).
      The StudyPlanCard / CoachsNote packet CTAs were never ported
      (`studyQueueRouteContract.test.tsx:1–6` documents this) — decide at
      the F7/R4 steer together with the StudyPlanCard orphan.
- [ ] **Delete the three never-emitted `SpotPacketWarning` literals**
      (`missing_original_hand_history`, `unsupported_large_field_icm`,
      `multiway_postflop_solver_limited`, `spotPacket.ts:91–118`) or emit
      them; and fix `game.type` asserting `'cash'` for non-tournament hands
      when `'unknown'` exists for that purpose (`spotPacket.ts:1116`).
- [ ] **Dedupe the Arena/Hands trainer-UI helper trio:** `formatBb` +
      `formatSourceValue` copied verbatim across `ArenaPage.tsx:289`,
      `SpotSourcePanel.tsx:88–95`, `TrainerSpotCard.tsx:24–31`; three
      *divergent* action→color maps (raise = emerald / violet / rose);
      two hand-maintained warning-label maps over the same union
      (`SpotSourcePanel.tsx:22–50` vs `ArenaPage.tsx:96–105`).
- [ ] **Fix the `.gitignore` graphify comment** ("regenerated by hooks" —
      no hook regenerates it; `settings.json` only runs
      `surface-open-reports.ts`) or add the hook, so scheduled reviews
      stop expecting a map that never exists in remote containers.
- [x] CLAUDE.md Key Dependencies block drift (vite 6→8, vitest 3→4,
      plugin-react 4→6, gsap/@gsap/react missing) — fixed in this PR.

## Codebase Health Summary

- **Overall health: good.** Full gate green in this container:
  `docs:check`, `typecheck`, `typecheck:test`, `lint` (0 errors,
  2 warnings), **846/846 tests (77 files, 33.7s)**, production build OK
  (PWA precache 61 entries, 2,478.7 KiB).
- **Main risks:** (1) R3/R4 shipped a well-tested but *unwired* feature
  layer — ~380 lines of study-queue bundle + SRS routing with zero
  production consumers, where passing tests mask that the feature doesn't
  exist in the app; (2) the god-file trend worsened (`spotPacket.ts` 1,288
  is now the largest file; `ArenaPage.tsx` 723→1,100; `HandsUpload.tsx`
  859→936); (3) abyss Waves 1–4 are entirely unstarted — every F-item
  checked (F3–F5, F8, F9, F14, F16–F29) is still open.
- **Highest-impact improvement:** run abyss Wave 1 (five of its items
  re-confirmed open here with exact locations), and make the wire-or-remove
  decision on the dead study-queue engine before more code accretes on it.
- **Confidence level:** high — all major claims verified against source or
  by executing the gate; two subagent claims were overruled by direct
  inspection (recorded below).

## Repository / Graphify Sync Status

- **Current branch:** `claude/relaxed-mccarthy-x2kkmv` (= `main` @ `9c4621e`).
- **Git status:** clean working tree before this report was added.
- **Uncommitted changes:** none.
- **Graphify freshness:** **absent.** `graphify-out/` was re-gitignored on
  2026-06-14 (PR #65) as a local-only tool; this fresh remote clone has no
  index, and the `.gitignore` claim that hooks regenerate it is inaccurate
  (no such hook exists). The last committed snapshot was already stale in
  the 2026-06-12 review.
- **Mismatches found:** n/a (no index to mismatch). Substitute map built
  from madge (circular deps), a per-module import fan-in sweep, LOC
  metrics, and `vite build` chunk sizes.
- **Files/modules changed locally but missing or stale in Graphify:** all
  of PRs #116–#122 (≈6,300 insertions, 58 files) postdate any Graphify
  index that ever existed — notably `src/analysis/spotPacket.ts`,
  `studyPacketProgress.ts`, `ungradedScenarios.ts`,
  `src/components/hands/SpotSourcePanel.tsx`, `TrainerSpotCard.tsx`.
- **Confidence impact:** none on conclusions (everything verified
  directly), but the "Graphify-assisted" premise of this scheduled review
  cannot hold in remote containers until the hook comment is made true or
  the review is re-scoped.

## Verification gate (this run)

| Check | Result |
|---|---|
| `npm run docs:check` | pass |
| `npm run typecheck` | pass |
| `npm run typecheck:test` | pass |
| `npm run lint` | pass — 0 errors, 2 `jsx-a11y` warnings (both the F22 dropzone) |
| `npm test` | **846 / 846** (77 files, 33.7s wall; env setup 57.7s cumulative) |
| `npm run build` | pass (precache 61 entries, 2,478.70 KiB) |

## Rebuilt map (Graphify substitute)

- **Entry points:** `src/main.tsx` → `App.tsx` (routes: `/` =
  CoachsNotePage, `/dashboard` = DashboardPage, `/coach` → redirect `/`);
  worker entry `src/parser/worker.ts`.
- **Fan-in hubs:** `types/analysis.ts` (58 importers), `types/hand.ts`
  (55) — shared domain types, hub-ness by design; `parser/pokerstars.ts`
  (22), `data/store.ts` (18), `analysis/leakDetector.ts` (15),
  `data/appStore.ts` / `analysis/rangeChecker.ts` (14), `parser/money.ts`
  (12).
- **Circular deps (madge):** exactly 1 — `types/analysis.ts →
  analysis/finalTableAnalyzer.ts` (= abyss F3; `types/analysis.ts:2–4`
  also imports from postflopAnalyzer and bountyAnalyzer — wrong direction
  even where acyclic).
- **Largest files:** `spotPacket.ts` 1,288 · `ArenaPage.tsx` 1,100 ·
  `HandsUpload.tsx` 936 · `store.ts` 932 · `HandReplay.tsx` 723 ·
  `CareerPage.tsx` 702 · `pokerstars.ts` 677. Production ≈25.1k LOC.
- **Largest route chunks:** SessionsPage 463.96 kB (149 gz, PDF stack) ·
  CareerPage 443.89 kB (125 gz) · index 375.94 kB · HandsPage 210 kB ·
  html2canvas 199.56 kB — unchanged since the abyss audit (F11–F13 open).
- **Orphans:** `StudyPlanCard.tsx`, `ValueSnapshotCard.tsx` (F7, still
  zero importers) — now joined by the unwired study-queue bundle/SRS
  routing exports (new finding above).

## Confirmed findings (delta vs 2026-07-01 abyss audit)

1. **NEW · High — Dead-in-production study-queue engine.** See Open items;
   verified by grep: zero non-test consumers for all six exports.
   Production uses only `buildSpotPacketFromParsedHand`
   (`ArenaPage.tsx:320`, `HandReplay.tsx:187`), `recordStudyPacketReview`
   + `studyPacketNextDueLabel` (`ArenaPage.tsx:13`), and the
   `ungradedScenarios` module (CareerPage/HandsPage).
2. **NEW · Medium — Phantom warning literals + `'cash'` mislabel** in
   `spotPacket.ts` (Open items). An honesty smell in the one module whose
   brand is honest refusal.
3. **NEW · Medium — Trainer-UI duplication across three files** (Open
   items). The divergent action-color palettes mean the same action is
   emerald in the Arena, violet in the source panel, rose on the card.
4. **NEW · Medium (fixed here) — CLAUDE.md Key Dependencies drift:** said
   vite 6 / vitest 3 / plugin-react 4; reality vite 8 / vitest 4 /
   plugin-react 6; `gsap` + `@gsap/react` missing from both the Stack line
   and the runtime list.
5. **NEW · Low — `.gitignore:35` graphify "regenerated by hooks" comment
   is false** (no hook; `settings.json` SessionStart only surfaces
   reports).
6. **Recurring/Worsened — god-files (F19):** `ArenaPage.tsx` 723→1,100
   (route parsing, SRS glue, drill state machine, 3 inline components,
   122-line `handleAction` at :555–677); `HandsUpload.tsx` 859→936;
   `store.ts` 910→932; plus new `spotPacket.ts` 1,288 (splittable at the
   packet/bundle seam, :402–1175 vs :1177+).
7. **Recurring — abyss Wave 1 items all open, locations refreshed:**
   F3 (`types/analysis.ts:4`); F4 (STATUS.md:30–31 routes table wrong —
   `/` is CoachsNotePage per `App.tsx:60`, `/coach` is a redirect;
   STATUS.md:228 still calls shipped FACING_3BET/B4+B5 "next";
   STATUS.md:37 still claims auto-classified archetypes, cut in #108);
   F5 (`@eslint/js` still undeclared, `eslint.config.js:1`);
   F20 (`HandsUpload.tsx:493` `window.confirm`); F22 (dropzone
   `HandsUpload.tsx:509–521`, the only 2 lint warnings); F23 **only half
   resolved** — "Pot 0.0" is still rendered (`ArenaPage.tsx:967–970`,
   label and literal in separate elements, which defeats a naive grep) and
   DrillCard's `color` prop is still declared/passed/never read
   (`ArenaPage.tsx:1042/1046`); F27 (probe/donk OOP check still missing,
   `postflopAnalyzer.ts:316–344`).
8. **Recurring — Waves 2–4 spot-checks all open:** F8 (all eight
   named exports still consumer-less; `handExists` → `store.ts:133`,
   `getVillainNote` → `store.ts:713` — both moved since the audit), F9
   (all 8 debris scripts + `agentKernel.test.ts` in `src/__tests__`), F14
   (equity in render IIFE, now `HandReplay.tsx:611–633`), F17 (~25 inline
   hex in career/dashboard — 6× `#15171f`, 5× `#a78bfa`, plus new
   raw-Tailwind-palette drift in SpotSourcePanel/TrainerSpotCard instead
   of `var(--*)` tokens), F24 (`'scorza23'` in `ggpoker.ts:30,37,349`,
   `tournamentSummary.ts:40`, `contributionPackage.ts:137`), F26/F28/F29
   (all micro-warts still at the audited lines).
9. **Resolved:** F1 — `SEVERITY_ACCENT` wired (`CoachsNotePage.tsx:31,93–94`),
   gate green (Wave 0 / #116 did land). June-review items stay closed:
   the `importRuns ↔ store` re-export knot is gone (importRuns now fronts
   only the leaf `importDiagnosticsPolicy`), and the STATUS.md
   verification stamp is autogen-enforced (`docs:check` green).
10. **Softened — F16 (jsdom env split):** still open (single
    `environment: 'jsdom'` in `vite.config.ts:42`), but this container
    measured 33.7s wall / 57.7s cumulative env — far below the audit's
    210s/458s (vitest 4 + faster hardware). Still correct, no longer "the
    biggest CI win".
11. **Not verifiable here — F10 (worktree debris):** lives on the owner's
    local machine; this fresh clone has no `.claude/worktrees`. Do not
    mark resolved from a remote container.

## Subagent claims overruled (verification discipline)

- "SpotSourcePanel/TrainerSpotCard have no dedicated tests" — **false**:
  `SpotSourcePanel.test.tsx` (277 lines) and `TrainerSpotCard.test.tsx`
  (126 lines) exist and are substantive.
- "Pot 0.0 resolved (no grep match)" — **false**: the label and the
  literal are split across two JSX elements (`ArenaPage.tsx:967–970`).

## Test-coverage notes

846 tests is a strong base and the R1–R5 code landed *with* tests
(spotPacket 726-line suite, route contract, dedicated component tests).
Gaps: the dead bundle path's own branches (`packet_limit_reached`,
`missing_hero_decision`, `empty_study_queue_bundle`) are untested — moot
if the code is removed; the §6 abyss util gaps (csvExport, pdfExport,
format.ts, strategyProfiles, appStore merge/migrate) remain as stated.

## Review Ledger

- **Date/time:** 2026-07-03 (scheduled remote session).
- **Trigger:** scheduled Graphify-assisted health review.
- **Branch:** `claude/relaxed-mccarthy-x2kkmv`. **Commit:** `9c4621e`.
- **Scope:** full repo; full gate executed; map rebuilt (madge + fan-in +
  LOC + bundle); 3 parallel inspection agents, all claims re-verified.
- **Graphify sync status:** absent (local-only tool, not regenerated in
  remote containers; gitignore hook claim false).
- **Files changed since last health review (2026-06-12, `36ffabd`):**
  ~30 PRs incl. the reskin era, #99–#122; this run's delta baseline is the
  abyss audit @ `39550c8`/`2489e4e` (58 files, +6,338/−393).
- **Areas inspected:** spotPacket/studyPacketProgress/ungradedScenarios
  (+tests), ArenaPage, SpotSourcePanel, TrainerSpotCard, HandsUpload,
  HandReplay, HandsPage, LeaksPage, CoachsNotePage, store.ts,
  importRuns.ts, types/analysis.ts, scenarioDetector, postflopAnalyzer,
  strategyProfiles, ranges.ts, eslint.config.js, vite.config.ts, App.tsx,
  STATUS.md, CLAUDE.md, .gitignore, .claude/settings.json, dist chunk
  sizes.
- **New findings:** dead study-queue engine (High); phantom warnings +
  cash mislabel; trainer-UI triplication; CLAUDE.md dep drift (fixed);
  gitignore hook-claim falsehood.
- **Recurring:** F3–F5, F8, F9, F14, F17, F20, F22, F23(half), F24,
  F26–F29 (all re-verified); god-files **worsened** (F19).
- **Resolved:** F1; June items (importRuns knot, STATUS stamp) hold.
- **Stale/soft:** F16 urgency downgraded by measurement; F10 unverifiable
  remotely; F23 "Pot 0.0" would false-resolve under naive grep — evidence
  recorded.
- **Recommended next actions:** see below.

## Recommended next actions

1. Decide wire-vs-remove for the study-queue bundle + SRS routing engine
   at the F7/R4 steer — unwired tested code is the most expensive kind of
   dead code — `spotPacket.ts`, `studyPacketProgress.ts`,
   `StudyPlanCard.tsx`.
2. Execute abyss Wave 1 (F3, F4, F5, F20, F22, F23-remainder, F27) — five
   items re-confirmed with fresh line numbers above; all are sub-hour —
   `types/analysis.ts`, `STATUS.md`, `package.json`, `HandsUpload.tsx`,
   `ArenaPage.tsx`, `postflopAnalyzer.ts`.
3. Extract a shared trainer-spot presentation module (formatters +
   action-color + warning-label maps) before the three copies drift
   further — `ArenaPage.tsx`, `SpotSourcePanel.tsx`, `TrainerSpotCard.tsx`.
4. Make the graphify story honest: add the regeneration hook or amend
   `.gitignore:35` and re-scope this scheduled review's premise —
   `.gitignore`, `.claude/settings.json`.
5. Fold `spotPacket.ts`'s packet/bundle split into the F19 decomposition
   list (it's now the repo's largest file) — `docs/reports/2026-07-01-abyss-audit.md`.
