---
date: 2026-06-15
author: claude
status: open
---

# PR State of Affairs — 2026-06-15

Snapshot taken at 22:25 UTC. 14 open PRs, all CI green (`lint / typecheck / test / build`).

---

## TL;DR

| State | Count | Action |
|---|---|---|
| Ready to merge (not draft, CI green) | 11 | Merge in order below; rebase after each conflict |
| Needs owner decision before merge | 1 | #78 — IndexedDB migration call |
| Codex drafts, lower priority | 3 | #79, #82, #83 |

---

## HOT — Merge queue (non-draft, CI green)

All verified passing. Merge in this order to minimise conflicts — several PRs touch `postflopAnalyzer.ts` and the audit report file, so whichever lands second on a shared file needs `git merge origin/main` + re-push.

| # | Title | Audit ticket | Files touched (hot-spot) | Priority |
|---|---|---|---|---|
| **#80** | Remove fabricated stats from sidebar + dashboard | CQ-1 | `Sidebar.tsx`, `DashboardPage.tsx` | CRITICAL — trust-killer |
| **#81** | Stop double-scaling villain VPIP/PFR | CQ-2 | `CareerPage.tsx` | HIGH — 100× display bug |
| **#84** | Don't wedge UI when import persistence fails | CQ-3 | `HandsUpload.tsx`, `worker.ts`, `workerProcessor.ts` | HIGH — blocking failure mode |
| **#85** | Surface skipped hand blocks to confidence ledger | CQ-4 | parser files, `workerProcessor.ts` | HIGH — silent bad confidence |
| **#86** | Require villain-was-PFR for BET_VS_MISSED_CBET | B2 | `postflopAnalyzer.ts` | MEDIUM — phantom leak |
| **#87** | Gate HU c-bet counter on hero in position | B1 | `postflopAnalyzer.ts` ⚠️ overlaps #86 | MEDIUM — stat contradiction |
| **#88** | Honest lifetime ROI + hourly-rate estimate | A5a | `careerStats.ts`, `CareerPage.tsx` | MEDIUM — fabricated derived stat |
| **#89** | Gate double-barrel opportunity on called c-bet | B3 | `postflopAnalyzer.ts` ⚠️ overlaps #86/#87 | MEDIUM — AF counter wrong |
| **#90** | Stop double-counting tournaments across session gap | A5b | `sessions.ts` | MEDIUM — PnL inflation |
| **#91** | Label ICM stage as estimate | B10 | `icmDetector.ts`, `METRICS_DICTIONARY.md` | LOW — display honesty |
| **#92** | Fix HandReplay legacy postflop fallback | B9 | `HandReplay.tsx`, `postflopAnalyzer.ts` | LOW — legacy data bug |

**Conflict risk map:**
- `postflopAnalyzer.ts`: #86, #87, #89, #92 — merge sequentially; rebase each after the previous lands
- Audit report file (`2026-06-12-principal-engineer-audit.md`): all batch PRs touch it — first one wins cleanly, the rest need a merge
- `CareerPage.tsx`: #81 and #88 both touch it — merge #81 first

---

## NOT (yet) — Drafts / blocked

### #78 — Parser chip accounting (EPIC A1 keystone) — DRAFT, CI green
**The most important fix in the queue.** Corrects raise investment and uncalled-bet handling — `netProfit`, `heroChipsAfter`, `villainDeltas`, and every bb-metric are wrong without it.

**Blocked on one owner decision** (called out in the PR):

> Historical data already in IndexedDB carries the old wrong `netProfit`. After this merges, freshly-imported hands disagree with old ones.

Options:
- **A (preferred by PR author):** Dexie migration that recomputes money fields from persisted action lists — fields are recomputable offline, no re-import needed.
- **B:** A "re-import recommended" banner keyed on import-run date — simpler, but shifts work to the user.

Once you pick A or B, this is a straight merge (CI green, failing-first tests, 3247/3257 conservation). The rest of the batch can merge before or after; they don't touch the parser's chip-tracking code.

---

### Codex drafts (lower priority)

| # | Title | Notes |
|---|---|---|
| **#79** | A11y + dashboard hygiene | Safe, no analysis files touched. Real BankrollChart date filtering, typed RingHud stat keys, `dangerouslySetInnerHTML` → plain text. CI green. |
| **#82** | Remove unused React Three deps | Pure dep cleanup (`three`, `@react-three/fiber`, `@react-three/drei`, `@types/three`). CI green. Safe to merge any time. |
| **#83** | Fix Arena C-bet Clinic drill | Feature fix — draw drill pool from `cbetOpportunity` hands, grade from postflop flags, replace `window.alert` with ConfirmDialog. CI green. |

---

## Open items

- [ ] Owner decides A vs B on #78 IndexedDB migration
- [ ] Merge #80 → #81 → #84 → #85 → #86 → #87 → #88 → #89 → #90 → #91 → #92 (rebase any conflicts)
- [ ] After #78 decision: implement migration / banner, merge #78
- [ ] Review + merge codex drafts (#82 is trivially safe; #79 and #83 deserve a read)
- [ ] Set `status: resolved` on `2026-06-12-principal-engineer-audit.md` once the critical items (CQ-1 through CQ-4, EPIC A1) are merged
