# STATUS — What's Actually Shipped

> **Purpose:** single source of truth for the current state of the code.
> Updated when reality changes, not when aspirations do.
> CLAUDE.md and ROADMAP.md describe *intent*; this file describes *fact*.

The source-derived inventory below is **autogenerated and enforced** by the
pre-commit `docs:check` hook, so route/dependency/test counts cannot silently
drift. This replaces the old hand-maintained "last verified" stamp that went
stale three reviews running (see
`docs/reports/archive/2026-06-12-codebase-health-review.md`, finding 3).

<!-- BEGIN:AUTOGEN:verified -->
**Source-derived inventory** (autogenerated by `npm run docs:update`, enforced
by the pre-commit `docs:check` hook — these counts cannot silently drift):

- **Test files:** 78
- **`it` / `test` calls (approximate):** 802

Run `npm test` for the live pass/fail tally. Dependency, route, source-tree,
and test inventories below are regenerated from source.
<!-- END:AUTOGEN:verified -->

---

## Routes actually wired (`src/App.tsx`, `Sidebar.tsx`)

| Path | Component | Notes |
|---|---|---|
| `/` | `DashboardPage` | ROI Total, ITM Rate, position heatmap, trend charts, session filter (local only) |
| `/coach` | `CoachsNotePage` | Coach's Note — focus leak + receipt hands + actionable Study Queue packet / Arena drill |
| `/hands` | `HandsPage` | Upload UI inline via `showUpload` toggle |
| `/stats` | `StatsPage` | ROI by buy-in, AF Total, Total ITMs |
| `/ranges` | `RangesPage` | 13×13 grid, Oracle/Mirror dual matrix |
| `/leaks` | `LeaksPage` | Leak list with severity |
| `/sessions` | `SessionsPage` | Session list + CSV/PDF export |
| `/villains` | `VillainsPage` | Auto-classified archetypes, manual notes |
| `/arena` | `ArenaPage` | Training drills |
| `/career` | `CareerPage` | Tournament history, ROI / ITM dashboard, profit timeline |
| `/demo` | `DemoPage` | Local synthetic demo seeding and private validation flow |

## Pages / files *not* in the repo (despite older docs saying so)

- `UploadPage.tsx` — doesn't exist; upload is inline in `HandsPage`.
- `ConfigPage.tsx` — doesn't exist; strategy profile selector is in `Sidebar.tsx`;
  hero name is persisted in the IndexedDB `settings` table.
- `Board.tsx` shared component — doesn't exist; HandReplay builds its own.
- Component subdirs `ranges/`, `stats/`, `leaks/`, `sessions/`, `upload/`,
  `villains/` — none exist.

## Dependencies actually installed (`package.json`)

The canonical dependency inventory is the autogenerated block below. Current
runtime families include React 19, React Router 7, Zustand, Dexie,
TanStack Table/Virtual, Recharts, Framer Motion, JSZip, jsPDF,
`poker-odds-calculator`, and `vite-plugin-pwa`.

Build/test families include Vite 8, TypeScript 5.7, Tailwind 4, ESLint 9,
Vitest 4, Testing Library, fake-indexeddb, and jsdom.

**Not installed** (despite older doc claims): `pokersolver`,
`@holdem-poker-tools/hand-matrix`.

## Analysis modules (`src/analysis/`)

The current non-test inventory is autogenerated in the source-tree block below.
Current analysis modules include parser-facing scenario detection, range
validation, postflop analysis, financials, career analysis, solver-boundary
scaffolding, study-plan generation, ungraded-scenario/refusal summaries, and
villain classification/cross-reference helpers.

## UI language

Target: English. As of 2026-05-11, 100% of UI strings, tooltips, and analysis-layer `note` strings have been purged of Portuguese. The app is fully localized to English.

## Correctness issue ledger

1. **W$SD side-pot inflation** — ✅ FIXED 2026-04-18. Parser
   (`pokerstars.ts`) now exposes `showdownWinners: Set<string>` populated
   from SUMMARY `Seat N: <name> (pos)? showed [cards] and won` lines.
   `scenarioDetector.ts:188` gates `wonAtShowdown` on Set membership, not
   on raw `wonAmount > 0`, so wins without showing (muck after villain
   exposed losing hand) no longer inflate W$SD. Regression tests in
   `scenarioDetector.test.ts` ("W$SD detection").
2. **AF has no leak alert** — ✅ FIXED 2026-04-18. `detectLeaks`
   (`leakDetector.ts`) now pushes an `af` leak when the aggregation factor
   falls outside `thresholds.af.min/max` (Game Plan + Advanced both use
   `[2, 3]`). Gated on `totalCalls >= 10 && (totalBets + totalRaises) >= 10`
   so the ratio is stable; severity computed via the shared
   `computeSeverity` helper.
3. **`rangeChecker.ts:152` silent null** — ✅ FIXED 2026-04-18.
   `checkFacingRaise` now emits `console.warn` when `openerPosition` is
   missing (hero position / hand / action logged), making parser
   seat-to-position drops visible. Still returns `null` to skip from
   compliance — conservative over inventing a fallback opener.
4. **Postflop spots unused** — ✅ FIXED 2026-04-18. Logic from
   `postflopAnalyzer.ts` now produces actionable leaks in `leakDetector.ts`
   categorized by strategy source ([Vol.2], [D#07], [D#21]).
5. **PDF hero name hardcoded** — ✅ FIXED 2026-04-17 (`utils/pdfExport.ts`
   now takes `heroName` param; `SessionsPage.tsx` passes from `useAppStore`).
6. **Session filter scope** — ✅ FIXED 2026-04-18. `activeSessionId` promoted
   to global Zustand store (`appStore.ts`); filtering now propagates to
   `DashboardPage`, `HandsPage`, and `StatsPage`.
7. **Non-standard variants under-covered** — ✅ FIXED 2026-06-14. Cap, Zoom,
   6+ Hold'em, and play-money each have dedicated assertions in
   `src/parser/__tests__/fixtureSweep.test.ts` › `specialized variant fixture
   checks`, on top of the broad PokerStars HH/TS sweeps. (The ggpoker `.zip`
   fixtures remain a separate `describe.todo` in the same file.)
8. **Villain aggregation atomicity** — ✅ FIXED 2026-05-11. The import path
   now handles villain aggregation failures with a repair route instead of
   leaving silent stale profiles.
9. **Range compliance MP / facing-raise fallback** — ✅ FIXED 2026-05-31.
   7/8-max `MP` RFI spots use a conservative MP1 baseline, Ranges page
   position filtering now applies to matrix decisions, and unsupported
   facing-raise raise/fold pairs skip instead of falling through to loose
   LP-vs-EP defaults.
10. **Large OHH JSON detection / upload worker cleanup** — ✅ FIXED
    2026-05-31. Site identification checks full JSON content for Open Hand
    History files before the 65KB text-signature scan, and superseded/unmounted
    upload workers are terminated.
11. **False c-bet opportunities and missed-cbet spots** — ✅ FIXED
    2026-05-31. Donk bets before hero acts block c-bet opportunities, hero
    preflop folds no longer produce postflop spots, OOP checks no longer count
    as mandatory missed c-bets, and flop sizing uses computed preflop pot.
12. **Villain `statsByPosition` persistence / raw counters** — ✅ FIXED
    2026-05-31. `statsByPosition` is now a serializable
    `Partial<Record<Position, PositionStats>>`, raw opportunity/action counters
    are persisted, and fake IndexedDB tests cover position stats plus 3-bet and
    c-bet denominators.
13. **Per-decision ICM compliance stage** - FIXED 2026-06-02. Advanced-profile
    BB suited-fold compliance now uses each `HeroDecision.icmStage` when present
    before falling back to the batch-level stage, so import and page
    recomputation no longer treats every hand as early game.
14. **HandReplay postflop consistency / note language** - FIXED 2026-06-02.
    HandReplay now prefers stored import-time `HeroDecision.postflopActions`
    over replay-time recomputation, and `postflopAnalyzer.ts` note regressions
    guard against the Portuguese fragments found in the June 1 refresh.
15. **Bounty/final-table context surfacing** - FIXED 2026-06-02.
    HandReplay now renders attached `bountyContext`, `fakeShoveSpot`, and
    `restealSpot` metadata in a Tournament Context panel instead of limiting
    visible cues to ICM stage and squeeze spots.
16. **PWA manifest assets** - FIXED 2026-06-02.
    The `public/` directory now contains the icons referenced by
    `vite.config.ts`: `favicon.ico`, `apple-touch-icon.png`, `masked-icon.svg`,
    `pwa-192x192.png`, and `pwa-512x512.png`.
17. **Exact `poker-odds-calculator` pin** - FIXED 2026-06-02.
    `package.json` and the root `package-lock.json` dependency spec now pin
    `poker-odds-calculator` to `0.4.0` instead of the pre-1.0 caret range.
18. **HandsPage test-only assertions** - FIXED 2026-06-02.
    `getHandCategory()` coverage moved from a self-executing test-environment
    guard inside `HandsPage.tsx` to `src/pages/__tests__/HandsPage.test.ts`.
19. **Recommendation trust labels / no-solver caveats** - FIXED 2026-06-04.
    Evidence metadata now carries recommendation strength, caveats, and
    `solverBacked` status. Leak Inbox and Study Queue show rule/proxy/reference
    labels, sample confidence, and no-solver-EV caveats so weak evidence is
    presented as a review prompt instead of solver-backed advice.
20. **Import diagnostics export** - FIXED 2026-06-04.
    Hands upload Data Health now exposes a Markdown diagnostics export with
    source filenames, aggregate import counts, confidence, and parser warnings.
    The export explicitly excludes raw hand histories, cards, actions, and
    player-level hand data so parse failures can be shared for support without
    dumping private hand content.
21. **Automatic local import diagnostics** - FIXED 2026-06-04.
    Import attempts now save structured local-only diagnostics automatically.
    Source filenames are reduced to basenames, parser warnings are single-line
    capped strings, old runs are pruned to the latest 50, and Data Health can
    clear diagnostics without deleting parsed hands. No network telemetry or
    silent upload path exists.
22. **Facing-raise reaction chart coverage** - FIXED 2026-06-04.
    Ranges now exposes a real opener selector for reaction spots instead of
    assuming CO for every hero position. The displayed hand sample, matrix, and
    coverage note use the same hero/opener pair, and the app labels rule-based,
    partial, or unsupported coverage rather than implying solver-backed charts.
23. **Open Hand History fixture sweep coverage** - FIXED 2026-06-05.
    The parser fixture sweep now includes two standardized Open Hand History
    JSON fixtures under `src/test/fixtures/ohh/` covering iPoker object-wrapper
    and 888/Pacific array-wrapper shapes. Native proprietary room text formats
    still require real samples before direct support is claimed.
24. **Private/local runtime guard** - FIXED 2026-06-06.
    Runtime external font/texture dependencies were removed, and
    `npm run privacy:check` now fails on unreviewed network APIs, remote runtime
    asset URLs, native share APIs, and known telemetry/cloud/payment/remote-AI
    SDK dependencies unless a future opt-in feature adds an explicit allowlist
    entry with matching privacy documentation.
25. **Import confidence ledger** - FIXED 2026-06-06.
    Local import diagnostics now derive a confidence ledger from retained import
    runs: analysis posture, parsed-file rate, high/medium/low run counts, saved
    record totals, and grouped parser warning categories. The upload Data Health
    card and Markdown diagnostics export use this ledger so downstream analysis
    can distinguish ready, directional, and needs-review import states without
    exposing raw hand histories or local paths.

## Recent landings (after the 2026-06-12 review snapshot)

- **Advanced strategy wired** (PR #72) — profile-aware c-bet and short-stack
  3-bet shove are now active under the Advanced profile (`postflopAnalyzer.ts`
  and the push/fold + range-checker paths).
- **Vite 6→8, Vitest 3→4, plugin-react 4→6** (PR #73) — reflected in the
  autogen deps block below.
- **Audit fix batch** (PRs #80–#92, merged 2026-06-18) — cleared most of the
  2026-06-12 principal-engineer audit:
  - UI honesty: removed fabricated sidebar/dashboard stats (CQ-1 #80), fixed the
    100× VPIP/PFR double-scale (CQ-2 #81) and the wedge-able import overlay
    (CQ-3 #84).
  - Import trust: per-hand parse failures now reach the confidence ledger
    (CQ-4 / A3 #85).
  - Leak credibility: villain-was-PFR gating for missed c-bet (B2 #86),
    position-aware HU c-bet (B1 #87), double-barrel gating (B3 #89), HandReplay
    legacy fallback (B9 #92), ICM stage labelled as an estimate (B10 #91).
  - Financials: honest lifetime ROI + hourly estimate (A5 #88) and the session
    tournament double-count fix (A5 #90).
  - Hygiene: unused 3D deps removed (#82), Arena C-bet Clinic drill fixed (#83).
- **Parser chip accounting — keystone (EPIC A1, PR #94, 2026-06-20)** —
  per-street committed-chips + `Uncalled bet … returned` handling in
  `pokerstars.ts` and `ggpoker.ts`. Net P&L, `villainDeltas`, and every
  bb-denominated metric are now correct in contested pots; corpus chip
  conservation rose 324 → 3048 / 3285 hands.
- **Chip-conservation invariant (EPIC A2)** — `fixtureSweep.test.ts` now asserts
  `Σ nets === −rake` per hand; sitting-out players are reincluded in
  `villainDeltas` so all 3285 hero-seated hands conserve (sit-out-hero dead-ante
  non-events are a documented carve-out).
- **SpotPacket trainer-prompt boundary** — `spotPacket.ts` emits a neutral,
  non-scored `trainerPrompt` with legal action menus from parsed hands or
  explicit trainer configs. These packets stay `study_packet_only`, warn when
  legal menus are inferred, and support mixed-action-ready trainer modeling
  without claiming solver-backed EV or answer scoring.
- **PKO missing-context SpotPacket taxonomy** — Bounty/PKO-sensitive packets now
  keep the same no-solver boundary while naming missing opponent bounty values,
  partial coverage context, potential multi-bounty all-in setup gaps, and
  PKO/pay-jump context gaps. `StudyPlan` mirrors those reasons in the Data
  Health source-context queue, and `SpotSourcePanel` renders the caveats without
  EV, trainer answers, or scoring claims.
- **SpotPacket payout/bounty context presence** — `SpotPacketTournament` can now
  record sanitized payout-table and opponent-bounty-table availability/source
  metadata. Payout and PKO bounty missing-context warnings clear only when the
  corresponding context is supplied; packets still stay `study_packet_only` and
  do not store opponent bounty values, solver EV, trainer answers, or scores.
- **SpotPacket external-review target config** — packets generated for external
  review targets such as HRC, ICMIZER, GTO Wizard, or Postflopizer now include an
  optional sanitized `externalReview` checklist: target label, present/missing
  model assumptions, not-attached result status, and a reproducible packet hash.
  The block is a setup/export aid only; it does not import solver output or
  upgrade the packet beyond `study_packet_only`.
- **Study Queue external-review bundle summary** — Study Queue `SpotPacket` bundle
  exports now aggregate external-review readiness into a sanitized bundle-level
  summary: target label, checklist coverage, missing-assumption counts, and
  not-attached result status. `StudyPlanCard` renders the same summary in the
  source/config drawer before export, without solver EV, frequencies, trainer
  answers/scoring, raw hand text, local paths, or villain names.
- **Study Queue suggested external-review targets** — SpotPackets now add
  suggested-only target-family hints to the external-review checklist: HRC and
  ICMIZER for ICM/PKO/all-in preflop review, GTO Wizard and Postflopizer for
  postflop tree/line review, and GTO Wizard plus HRC for generic preflop setup
  review. The dashboard drawer and exported bundle carry those hints as
  metadata only: no auto-upload, imported solver result, EV/frequency output,
  trainer answer/scoring, raw hand text, local path, or villain name is added,
  and packets remain `study_packet_only`.
- **Hand Replay selected-packet external-review hints** — Hand Replay now builds
  selected-hand `SpotPacket`s with the same sanitized external-review setup
  checklist used by Study Queue bundles, and `SpotSourcePanel` renders
  suggested-only target-family hints for the current hand. Single-hand JSON
  exports can therefore carry HRC/ICMIZER, GTO Wizard/Postflopizer, or generic
  preflop setup suggestions without auto-uploading, attaching solver EV,
  importing frequencies, exposing trainer answers/scoring, or including raw hand
  text, local paths, or villain names.
- **Hand Replay SpotSourcePanel** — `HandReplay` now renders a local-only study
  packet panel from each `SpotPacket`, including evidence labels, legal-action
  menu, prioritized caveats, table-stack risk context, packet ID, and explicit
  no-solver/no-EV/no-scoring copy. The panel intentionally uses anonymized packet
  metadata rather than raw hand text or villain names.
- **Hand Replay TrainerSpotCard** — `HandReplay` now renders a neutral local
  drill prompt from the same sanitized `SpotPacket`. The card shows hero hand,
  scenario, pot/table context, anonymized seat stacks, preflop action path, and
  legal actions with stable selectors, but no answer buttons, trainer scoring,
  solver EV, or raw villain names.
- **Import source metadata for SpotPackets** — upload/worker imports persist a
  safe per-hand `importSource` (site, file type, local/ZIP access method, parser
  confidence) without filenames, local paths, raw hand text, or player names.
  `SpotPacket` and the Hand Replay source panel now use that stored metadata
  unless an explicit packet source override is supplied.
- **Selected-hand SpotPacket JSON export** — the Hand Replay `SpotSourcePanel`
  now offers a local-only JSON download for the current hand's sanitized
  `SpotPacket`. The export keeps the no-solver/no-EV/no-scoring boundary and
  excludes raw hand histories, filenames/paths, and villain names.
- **Source-aware import guide** — `HandsUpload` now shows a local import guide
  for PokerStars, GGPoker/PokerCraft, Open Hand History JSON, and recognized
  unsupported rooms. The copy routes proprietary room text formats toward OHH or
  sanitized samples and keeps unsupported sources as study prompts rather than
  fake solver-ready data.
- **Study Queue SpotPacket bundle export** — `DashboardPage` now wires the ranked
  Study Queue into a local-only `spot-packet-bundle/v1` JSON export through
  `StudyPlanCard`. The bundle preserves queue priority order, dedupes repeated
  hands, records missing hand/decision context, and exports sanitized
  `SpotPacket`s without raw hand histories, villain names, solver EV, or trainer
  answers.
- **Study Queue SpotPacket source/config drawer** — `StudyPlanCard` now shows a
  compact bundle drawer before export with the local-only target/evidence label,
  packet coverage, next-actionable hero/scenario/source snapshot or no-actionable
  cue, warning summary, local marker controls, and omission reasons. The drawer
  keeps the same no-solver/no-trainer-scoring boundary and does not expose raw
  hand text, local paths, or villain names.
- **Study Queue next-packet Hand Replay link** — `StudyPlanCard` now links the
  next sanitized Study Queue packet to `/hands?panel=spot-packet&reviewHand=…`,
  and `HandsPage` opens the requested local hand in `HandReplay` when it exists.
  That connects the dashboard queue to the existing `SpotSourcePanel` and
  `TrainerSpotCard` without adding solver EV, trainer answers/scoring, raw hand
  text, local paths, or villain names.
- **Study Queue local packet-progress markers** — `StudyPlanCard` now stores
  browser-local reviewed/starred/snoozed markers for the next sanitized
  SpotPacket and summarizes bundle progress. The next-packet CTA advances past
  reviewed or snoozed packets when another local packet is available, so a study
  block can continue without re-opening the same marked spot. The markers are
  convenience study metadata only: they do not store answers, trainer scores,
  solver EV, raw hand text, local paths, or villain names.
- **Study Queue local SRS routing** — Reviewed Study Queue SpotPackets now get a
  browser-local spaced-repeat due date and repetition count. Due reviewed packets
  route back into the next-packet CTA before untouched packets, while reviewed
  packets with future SRS dates stay out of multi-packet Arena routes until due
  and snoozed packets stay skipped. When every packet is snoozed or waiting on a
  future SRS date, Dashboard and Coach withhold Hand Replay/Arena packet CTAs
  instead of routing a skipped fallback as actionable, while the dashboard drawer
  keeps a selected skipped marker available for unsnooze/reset recovery. The
  dashboard exposes an outside-the-drawer progress overview with
  reviewed/starred/snoozed/due-now counts, next-packet or no-actionable SRS copy,
  and a reset cue; the drawer reset control clears only the selected packet's
  browser-local progress/SRS marker so a user can restart that packet without
  deleting hands, answers, scores, EV, or raw hand data. The cadence is local
  study metadata only and still does not store trainer answers, scores, solver
  EV, raw hand text, local paths, or villain names.
- **Study Queue Arena packet drill bridge** — `StudyPlanCard` now links the next
  sanitized Study Queue packet into `/arena?drill=study-queue&handId=…`; when a
  due/next bundle has multiple actionable packets, the Arena route also carries
  ordered `handIds`/`packetIds` for a multi-packet local session. `ArenaPage`
  auto-starts the exact imported hero decisions in route order, rehydrates each
  local parsed hand into a sanitized `SpotPacket`, shows the packet legal-action
  menu plus source/caveat warnings, records browser-local SRS progress after each
  prompt, shows a completed-session summary with reviewed count, next due cue,
  dashboard return, and last-packet Hand Replay link, and keeps all-in or
  ungraded/refusal spots as “Review only” no-score prompts, preserving the
  no-solver-EV/no-trainer-answer boundary.
- **Coach's Note Study Queue packet focus** — `/coach` now reuses the same
  local Study Queue `SpotPacket` bundle and browser-local SRS router as the
  dashboard, then surfaces only a due/untouched actionable packet beside the
  focus leak with Hand Replay and Arena drill links. When no packet is actionable
  because all local markers are snoozed or waiting on future SRS dates, Coach
  falls back to the generic Arena CTA instead of implying a skipped packet is due.
  When the selected bundle has multiple actionable packets, the Coach-to-Arena
  link preserves ordered `handIds` and `packetIds` for the same multi-packet
  local drill session used by the dashboard. The coach panel keeps the packet
  study-only/no-EV boundary explicit and does not store solver EV, trainer
  answers, trainer scores, raw hand text, local paths, or villain names.
- **Data Health study-queue source/context item** — `buildStudyQueue` now adds a
  review-only Data Health item when queued hands have legacy/unknown import
  source metadata, directional/low parser confidence, unsupported/unknown source
  labels, or ICM/bounty tournament-context caveats. The item links those hand IDs
  into the local SpotPacket bundle boundary while staying explicitly
  unsupported/no-EV/no-trainer-scoring.
- **Dashboard Data Health drill-down** — Dashboard Study Queue data-health items
  now show retained import-run warning categories and parser-confidence ledger
  context from local diagnostics, then deep-link to `/hands?panel=data-health#data-health`.
  The Hands page opens the local importer automatically for that link and anchors
  the Data Health panel without exposing raw hand histories or local paths.
- **Leaks Data Health refusal notice** — `LeaksPage` now expands low/medium local
  import confidence into an explicit refusal/triage notice before the leak list:
  blocked vs directional posture, parsed-file rate, failed-file count, saved
  records, top warning categories, and a deep-link to the Data Health panel. This
  makes unsupported/unparsed sources visible in the leak-review surface instead
  of letting omitted or biased samples look clean.
- **BB multiway defense trust boundary** — `detectScenario` now separates BB hands
  facing a normal open plus at least one caller/limper before hero as
  `BB_VS_RAISE_MULTIWAY`. Range compliance excludes that family from the heads-up
  BB suited-fold rule. `SpotPacket.preflopContext` records opener position,
  caller positions, call/limp role, squeeze/iso review flags, and the Hand Replay
  source panel surfaces those details with a `bb_multiway_defense_context` caveat
  while staying study-packet-only and not solver-backed.
- **Hands not-graded scenario aggregate/filters** — `HandsPage` now aggregates
  refusal scenarios (`FACING_3BET`, `FACING_ALL_IN`, `BB_VS_RAISE_MULTIWAY`,
  `BB_VS_LARGE_RAISE`, `BB_VS_LIMP`) into a session-scoped “Not graded review
  queue” with fold/continue counts and one-click filters. The Compliance filter
  also includes “Not graded,” keeping these hands visible as study/export spots
  without marking them as leaks or solver-backed verdicts.
- **Career/Stats not-graded caveat** — the unified Career stats hub now reuses a
  shared ungraded-scenario summary to show when compliance-refusal spots are
  present in aggregate stats. The notice reports not-graded rate, gradeable
  denominator, fold/continue split, top scenario families, and a Hand Archive
  route while keeping those decisions review-only, not solver-backed leak
  verdicts.
- **Facing-raise dynamic not-graded routing** — `FACING_RAISE` spots that return
  `null` for decision-specific reasons are now explicit review-only items instead
  of silent skips: unknown opener position, BTN/BB flat calls, and unsupported
  hero/opener reaction pairs receive reasons in Hand Replay and flow into Hands /
  Career not-graded aggregates. Non-BTN/BB cold-calls with known openers remain
  graded deviations.
- **Range validation reference-label cleanup** — `RangeValidatorPanel`, Range
  Matrix Oracle/Mirror copy, range validation notes, Leak Inbox evidence, and
  Study Queue evidence now describe internal range percentages as documented
  chipEV reference/study-table baselines rather than solver output, GTO Standard,
  automatic open/fold commands, or private-tool style. Exact bounty/ICM
  validation remains an external calculator/export workflow, not a local solver
  claim.
- **Cross-surface reference-copy cleanup** — Session History, Career current form,
  Arena drill feedback/profile badges, Hand Archive filters, Hand Replay verdicts,
  Lifetime scorecard, leak evidence metadata, and leak source labels now use
  local reference/baseline wording instead of GTO-compliance or absolute
  theoretical-range copy. The copy keeps local range checks useful while avoiding
  solver-backed EV or trainer-answer implications.
- **Hand Replay/Data Health stable selector anchors** — Hand Replay now exposes
  owned `hand-replay-*` anchors around the dialog and study-packet review
  surface, while HandsUpload exposes owned `hands-upload-*` anchors for the
  uploader root, dropzone, and Data Health entry. These selectors make the local
  Study Queue → Hand Replay/Data Health smoke path browser-testable without
  relying on private study-tool tabs, screenshots, raw hand histories, local
  paths, villain names, solver EV, trainer answers, or trainer scores.

## Open follow-ups

The current prioritised punch list lives in `ROADMAP.md` › **Active Covenant**
(Act I correctness → Act II coach loop). Headline open items:

- **Act I correctness is no longer waiting on B4/B5 in this branch.**
  `FACING_3BET` is classified as its own ungraded/refusal scenario until real
  3-bet-defense / 4-bet ranges exist, and postflop leak detection now uses
  gradeable-opportunity error rates instead of raw error counts.
- **Act II coach-loop work is the next product lane:** demote dashboard-only
  reporting toward “what should I study now?”, continue from the browser-local
  Study Queue SRS loop toward Arena drills from the user's own misplayed spots,
  and keep refusal-as-UI aggregates current as new review-only scenario families
  are discovered.
- Solver-validated per-pair facing-raise charts remain a strategy-data
  follow-up. Current reaction coverage is explicit and rule-based, not
  solver-backed.
- Native proprietary room text formats such as iPoker, 888/Pacific, WPN,
  PartyPoker, Chico, and Winamax remain unsupported unless converted to
  standardized Open Hand History JSON or backed by real parser fixtures.

## Repo hygiene

Layout is documented in the root `README.md` and `docs/README.md`.
`docs/product/STATUS.md` remains the shipped-state source of truth, and the
autogenerated inventory below is the safest source for current routes, source
files, dependencies, and test files.

## Code + UX audit (Batch 1 — 2026-04-18)

Completed (quick wins):
- RangesPage `RangeValidatorPanel` Portuguese → English (12 strings)
- Dead state removed: `dateFrom`/`dateTo` useState + date filter inputs in HandsPage
- `aria-label` added to all icon-only buttons (HandsPage, HandReplay, ConfirmDialog)
- 4 hardcoded hex colors → CSS variables (`--color-bg-dialog`, `--color-bg-tooltip`,
  `--color-bg-board`, `--color-bg-card-solid` in `@theme` block)

Remaining (Batch 2 — medium effort):
- [x] Dialog accessibility: `role="dialog"`, `aria-modal`, Esc key, focus trap (Completed 2026-05-11)
- [x] Analysis-layer Portuguese purge: 7 files + test assertions (Completed 2026-05-11)
- [x] Worker error handling: post `FILE_ERROR` from worker, `WorkerMessage` union type;
  `COMPLETE` now includes an import-confidence summary (`high`/`medium`/`low`, parsed/failed file counts, capped warnings), and upload UI renders the confidence badge after processing (Completed 2026-05-15)
- [x] Villain aggregation atomicity (`store.ts` txn boundary) (Completed 2026-05-11)

Remaining (Batch 3 — structural):
- [x] Route-level code splitting (React.lazy + Suspense) (Completed 2026-05-11)
- [x] Component smoke tests (happy-dom + @testing-library/react) (Completed 2026-05-12)
- [x] HandsPage decomposition (683 lines → extract hooks/components, added TanStack Virtual) (Completed 2026-05-11)
- [x] DualRangeMatrix cell memoization (Completed 2026-05-24)
- [x] DashboardPage query optimization (split monolithic useLiveQuery) (Completed 2026-05-11)

---

## Machine-generated snapshots

These blocks are regenerated by `scripts/regen-status.ts` (run via
`npm run docs:update`). The pre-commit hook refuses to commit if they
are stale. Do not edit by hand.

### Dependencies

<!-- BEGIN:AUTOGEN:deps -->
**Runtime** (`dependencies`):

- @gsap/react ^2.1.2
- @tanstack/react-table ^8.21.3
- @tanstack/react-virtual ^3.13.24
- clsx ^2.1.1
- date-fns ^4.2.1
- dexie ^4.4.2
- dexie-react-hooks ^4.4.0
- framer-motion ^12.39.0
- gsap ^3.15.0
- jspdf ^4.2.1
- jspdf-autotable ^5.0.8
- jszip ^3.10.1
- lucide-react ^1.16.0
- poker-odds-calculator 0.4.0
- react ^19.2.6
- react-dom ^19.2.6
- react-router-dom ^7.15.1
- recharts ^3.8.1
- zustand ^5.0.13

**Build / test** (`devDependencies`):

- @tailwindcss/vite ^4.2.2
- @testing-library/jest-dom ^6.9.1
- @testing-library/react ^16.3.2
- @types/node ^22.19.19
- @types/react ^19.0.0
- @types/react-dom ^19.0.0
- @vitejs/plugin-react ^6.0.0
- @vitest/coverage-v8 ^4.0.0
- eslint ^9.39.4
- eslint-plugin-jsx-a11y ^6.10.2
- eslint-plugin-react ^7.37.5
- eslint-plugin-react-hooks ^5.2.0
- fake-indexeddb ^6.2.5
- globals ^15.15.0
- jsdom ^29.1.1
- tailwindcss ^4.2.2
- tsx ^4.21.0
- typescript ~5.7.2
- typescript-eslint ^8.59.3
- vite ^8.0.0
- vite-plugin-pwa ^1.3.0
- vitest ^4.0.0
<!-- END:AUTOGEN:deps -->

### Wired routes (`src/App.tsx`)

<!-- BEGIN:AUTOGEN:routes -->
| Path | Component |
|---|---|
| `/` | `DashboardPage` |
| `/arena` | `ArenaPage` |
| `/career` | `CareerPage` |
| `/coach` | `CoachsNotePage` |
| `/demo` | `DemoPage` |
| `/hands` | `HandsPage` |
| `/leaks` | `LeaksPage` |
| `/ranges` | `RangesPage` |
| `/sessions` | `SessionsPage` |
| `/stats` | `StatsPage` |
| `/villains` | `VillainsPage` |
<!-- END:AUTOGEN:routes -->

### Source tree

<!-- BEGIN:AUTOGEN:src-tree -->
```
src/parser/  (14 files)
  parser/buyInExtractor.ts
  parser/contributionPackage.ts
  parser/ggpoker.ts
  parser/handKey.ts
  parser/importSummary.ts
  parser/money.ts
  parser/openHandHistory.ts
  parser/pokerstars.ts
  parser/position.ts
  parser/sanitizeHandHistory.ts
  parser/siteIdentifier.ts
  parser/tournamentSummary.ts
  parser/worker.ts
  parser/workerProcessor.ts
src/analysis/  (26 files)
  analysis/bountyAnalyzer.ts
  analysis/careerCoach.ts
  analysis/careerScope.ts
  analysis/careerStats.ts
  analysis/coachsNote.ts
  analysis/finalTableAnalyzer.ts
  analysis/financials.ts
  analysis/headsUpPushFoldReference.ts
  analysis/icmDetector.ts
  analysis/leakDetector.ts
  analysis/leakLifecycle.ts
  analysis/math.ts
  analysis/positionStats.ts
  analysis/postflopAnalyzer.ts
  analysis/proofHandSelector.ts
  analysis/pushFoldChecker.ts
  analysis/rangeChecker.ts
  analysis/rangeValidator.ts
  analysis/scenarioDetector.ts
  analysis/solverAdapter.ts
  analysis/spotPacket.ts
  analysis/squeezeDetector.ts
  analysis/studyPacketProgress.ts
  analysis/studyPlan.ts
  analysis/ungradedScenarios.ts
  analysis/villainClassifier.ts
src/data/  (12 files)
  data/appStore.ts
  data/demoDataset.ts
  data/demoVillains.ts
  data/importDiagnosticsPolicy.ts
  data/importRuns.ts
  data/localHeadsUpReferences.ts
  data/localStorage.ts
  data/pushFoldRanges.ts
  data/ranges.ts
  data/sessions.ts
  data/store.ts
  data/strategyProfiles.ts
src/pages/  (11 files)
  pages/ArenaPage.tsx
  pages/CareerPage.tsx
  pages/CoachsNotePage.tsx
  pages/DashboardPage.tsx
  pages/DemoPage.tsx
  pages/HandsPage.tsx
  pages/LeaksPage.tsx
  pages/RangesPage.tsx
  pages/SessionsPage.tsx
  pages/StatsPage.tsx
  pages/VillainsPage.tsx
src/components/  (35 files)
  components/career/BustOutChart.tsx
  components/career/CareerCoachCard.tsx
  components/career/CareerDashboard.tsx
  components/career/CareerScopePanel.tsx
  components/career/CareerStreaksCard.tsx
  components/career/DayHourHeatmap.tsx
  components/career/FormatBreakdownTable.tsx
  components/career/LifetimeScorecard.tsx
  components/career/StakeTrendChart.tsx
  components/career/TimelineFeed.tsx
  components/dashboard/BankrollChart.tsx
  components/dashboard/MonumentCurve.tsx
  components/dashboard/PositionalHeatmap.tsx
  components/dashboard/RingHud.tsx
  components/dashboard/StudyPlanCard.tsx
  components/dashboard/TrendChart.tsx
  components/dashboard/ValueSnapshotCard.tsx
  components/dashboard/VerdictGauge.tsx
  components/dashboard/WireTape.tsx
  components/hands/HandReplay.tsx
  components/hands/HandsFilters.tsx
  components/hands/HandsTable.tsx
  components/hands/HandsUpload.tsx
  components/hands/SpotSourcePanel.tsx
  components/hands/TrainerSpotCard.tsx
  components/layout/ErrorBoundary.tsx
  components/layout/Layout.tsx
  components/layout/Sidebar.tsx
  components/shared/Card.tsx
  components/shared/ConfirmDialog.tsx
  components/shared/DemoDataButton.tsx
  components/shared/DualRangeMatrix.tsx
  components/shared/InfoTooltip.tsx
  components/shared/RangeGrid.tsx
  components/shared/StatCard.tsx
src/utils/  (4 files)
  utils/csvExport.ts
  utils/evidence.ts
  utils/format.ts
  utils/pdfExport.ts
src/types/  (5 files)
  types/analysis.ts
  types/evidence.ts
  types/hand.ts
  types/ranges.ts
  types/villain.ts
```
<!-- END:AUTOGEN:src-tree -->

### Tests

<!-- BEGIN:AUTOGEN:tests -->
**Test files:** 78
**`it` / `test` calls (approximate):** 802

```
src/__tests__/App.test.tsx
src/__tests__/agentKernel.test.ts
src/__tests__/privacyBoundaryCheck.test.ts
src/__tests__/studyQueueRouteContract.test.tsx
src/analysis/__tests__/bountyAnalyzer.test.ts
src/analysis/__tests__/careerCoach.test.ts
src/analysis/__tests__/careerScope.test.ts
src/analysis/__tests__/careerStats.test.ts
src/analysis/__tests__/coachsNote.test.ts
src/analysis/__tests__/finalTableAnalyzer.test.ts
src/analysis/__tests__/financials.test.ts
src/analysis/__tests__/headsUpPushFoldReference.test.ts
src/analysis/__tests__/icmDetector.test.ts
src/analysis/__tests__/leakDetector.test.ts
src/analysis/__tests__/leakLifecycle.test.ts
src/analysis/__tests__/math.test.ts
src/analysis/__tests__/positionStats.test.ts
src/analysis/__tests__/postflopAnalyzer.test.ts
src/analysis/__tests__/proofHandSelector.test.ts
src/analysis/__tests__/pushFoldChecker.test.ts
src/analysis/__tests__/rangeChecker.test.ts
src/analysis/__tests__/rangeValidator.test.ts
src/analysis/__tests__/scenarioDetector.test.ts
src/analysis/__tests__/solverAdapter.test.ts
src/analysis/__tests__/solverSpotBuilder.test.ts
src/analysis/__tests__/spotPacket.test.ts
src/analysis/__tests__/squeezeDetector.test.ts
src/analysis/__tests__/studyPacketProgress.test.ts
src/analysis/__tests__/studyPlan.test.ts
src/analysis/__tests__/ungradedScenarios.test.ts
src/analysis/__tests__/villainClassifier.test.ts
src/components/career/__tests__/CareerStreaksCard.test.tsx
src/components/career/__tests__/FormatBreakdownTable.test.tsx
src/components/career/__tests__/LifetimeScorecard.test.tsx
src/components/dashboard/__tests__/StudyPlanCard.test.tsx
src/components/dashboard/__tests__/TrendChart.test.tsx
src/components/hands/__tests__/HandReplay.test.tsx
src/components/hands/__tests__/HandsFilters.test.tsx
src/components/hands/__tests__/HandsUpload.test.tsx
src/components/hands/__tests__/SpotSourcePanel.test.tsx
src/components/hands/__tests__/TrainerSpotCard.test.tsx
src/components/layout/__tests__/Sidebar.test.tsx
src/components/shared/__tests__/Card.test.tsx
src/components/shared/__tests__/ConfirmDialog.test.tsx
src/components/shared/__tests__/DualRangeMatrix.test.tsx
src/components/shared/__tests__/InfoTooltip.test.tsx
src/components/shared/__tests__/RangeGrid.test.tsx
src/components/shared/__tests__/StatCard.test.tsx
src/data/__tests__/demoDataset.test.ts
src/data/__tests__/demoSeedProgress.test.ts
src/data/__tests__/importRuns.test.ts
src/data/__tests__/localHeadsUpReferences.test.ts
src/data/__tests__/localStorage.test.ts
src/data/__tests__/pushFoldRanges.test.ts
src/data/__tests__/ranges.test.ts
src/data/__tests__/sessions.test.ts
src/data/__tests__/store.test.ts
src/pages/__tests__/ArenaPage.test.tsx
src/pages/__tests__/CoachsNotePage.test.tsx
src/pages/__tests__/HandsPage.test.ts
src/pages/__tests__/LeaksPage.test.tsx
src/parser/__tests__/buyInExtractor.test.ts
src/parser/__tests__/chipAccounting.test.ts
src/parser/__tests__/contributionPackage.test.ts
src/parser/__tests__/fixtureSweep.test.ts
src/parser/__tests__/ggpoker.test.ts
src/parser/__tests__/ggpoker_robustness.test.ts
src/parser/__tests__/handKey.test.ts
src/parser/__tests__/importSummary.test.ts
src/parser/__tests__/money.test.ts
src/parser/__tests__/openHandHistory.test.ts
src/parser/__tests__/pokerstars.test.ts
src/parser/__tests__/position.test.ts
src/parser/__tests__/sanitizeHandHistory.test.ts
src/parser/__tests__/siteIdentifier.test.ts
src/parser/__tests__/uploadSizeGuards.test.ts
src/parser/__tests__/workerImportSummary.test.ts
src/utils/__tests__/evidence.test.ts
```
<!-- END:AUTOGEN:tests -->

---

## Update protocol

When shipping a change:

1. If the change affects structure, deps, routes, or language status,
   update this file in the same commit.
2. If the change fixes a "Known correctness issue" above, mark it
   ✅ FIXED with the commit date; don't delete the entry for 30 days
   (so PR reviewers know what shifted).
3. If the change adds a new phantom (e.g. a route that's not yet wired),
   add a `⚠️ PARTIAL` row, not a green ✅.

CLAUDE.md and AGENTS.md should reference this file rather than re-stating
structure or dep lists. This file stays identical to the code.
